{
  List<String> args=new ArrayList<>();
  if (!classPath.isEmpty()) {
    args.add("-cp");
    args.add(classPath);
  }
  args.add("-d");
  args.add(build.getClassDir());
  args.add("-sourcepath");
  args.add(":");
  return args;
}
