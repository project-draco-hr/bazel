{
  if (this == other) {
    return true;
  }
  if (!(other instanceof NativeAspectKey)) {
    return false;
  }
  NativeAspectKey that=(NativeAspectKey)other;
  return Objects.equal(label,that.label) && Objects.equal(configuration,that.configuration) && Objects.equal(aspect,that.aspect);
}
