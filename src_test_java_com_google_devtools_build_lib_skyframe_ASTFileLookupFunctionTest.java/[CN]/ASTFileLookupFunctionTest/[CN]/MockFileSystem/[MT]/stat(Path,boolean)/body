{
  if (statThrowsIoException && path.asFragment().getPathString().equals("/workspace/tools/build_rules/prelude_" + Constants.PRODUCT_NAME)) {
    throw new IOException("bork");
  }
  return super.stat(path,followSymlinks);
}
