{
  graph.getSuccessfulValues(Iterables.transform(traversals,new Function<TraversalInfo,SkyKey>(){
    @Override public SkyKey apply(    TraversalInfo traversalInfo){
      return PackageValue.key(PackageIdentifier.create(repositoryName,traversalInfo.rootedDir.getRelativePath()));
    }
  }
));
}
