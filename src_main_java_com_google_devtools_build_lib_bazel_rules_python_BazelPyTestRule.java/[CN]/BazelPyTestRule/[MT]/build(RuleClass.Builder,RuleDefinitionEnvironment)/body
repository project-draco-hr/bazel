{
  return builder.override(attr("testonly",BOOLEAN).value(true).nonconfigurable("policy decision: should be consistent across configurations")).override(attr("stamp",TRISTATE).value(TriState.NO)).build();
}
