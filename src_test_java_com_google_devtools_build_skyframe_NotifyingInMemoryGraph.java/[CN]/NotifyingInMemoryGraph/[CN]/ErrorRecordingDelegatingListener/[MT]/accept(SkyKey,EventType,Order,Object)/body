{
  try {
    delegate.accept(key,type,order,context);
  }
 catch (  Exception e) {
    TrackingAwaiter.INSTANCE.injectExceptionAndMessage(e,"In NotifyingInMemoryGraph: " + Joiner.on(", ").join(key,type,order,context));
    throw e;
  }
}
