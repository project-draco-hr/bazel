{
  Iterable<LibraryToLink> allLibraries=Iterables.concat(staticLibraries,picStaticLibraries,dynamicLibraries,executionDynamicLibraries);
  ImmutableList.Builder<LibraryToLink> result=new ImmutableList.Builder<>();
  for (  LibraryToLink library : allLibraries) {
    if (libraryIdentifierOf(library.getOriginalLibraryArtifact()).equals(libraryIdentifierOf(input.getOriginalLibraryArtifact()))) {
      result.add(library);
    }
  }
  return result.build();
}
