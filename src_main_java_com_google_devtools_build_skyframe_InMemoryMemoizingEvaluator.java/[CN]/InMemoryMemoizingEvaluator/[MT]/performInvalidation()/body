{
  EagerInvalidator.delete(graph,valuesToDelete,progressReceiver,deleterState,keepEdges,dirtyKeyTracker);
  valuesToDelete=new LinkedHashSet<>();
  EagerInvalidator.invalidate(graph,valuesToDirty,progressReceiver,invalidatorState,dirtyKeyTracker);
  valuesToDirty=new LinkedHashSet<>();
}
