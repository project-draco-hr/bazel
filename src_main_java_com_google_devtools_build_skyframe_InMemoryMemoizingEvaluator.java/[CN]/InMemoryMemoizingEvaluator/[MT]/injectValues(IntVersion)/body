{
  if (valuesToInject.isEmpty()) {
    return;
  }
  for (  Entry<SkyKey,SkyValue> entry : valuesToInject.entrySet()) {
    ParallelEvaluator.injectValue(entry.getKey(),entry.getValue(),version,graph,dirtyKeyTracker);
  }
  valuesToInject=new HashMap<>();
}
