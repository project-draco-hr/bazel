{
  try {
    return ImmutableSet.copyOf(dependentNodeMap(node,hostConfig,null,AspectParameters.EMPTY,configConditions).values());
  }
 catch (  EvalException e) {
    throw new IllegalStateException(e);
  }
}
