{
  Label label=Label.parseAbsolute("//x");
  assertEquals("FilesetEntry(srcdir = \"//x:x\"," + " files = [\"//x:x\"]," + " excludes = [],"+ " destdir = \"\","+ " strip_prefix = \".\","+ " symlinks = \"copy\")",EvalUtils.prettyPrintValue(new FilesetEntry(label,Arrays.asList(label),Arrays.<String>asList(),"",FilesetEntry.SymlinkBehavior.COPY,".")));
}
