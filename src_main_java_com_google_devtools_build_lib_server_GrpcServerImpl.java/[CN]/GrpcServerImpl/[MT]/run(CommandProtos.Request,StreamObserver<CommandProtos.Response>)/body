{
  Preconditions.checkState(serving);
  commandExecutor.exec(ImmutableList.of("version"),OutErr.SYSTEM_OUT_ERR,clock.currentTimeMillis());
  CommandProtos.Response response=CommandProtos.Response.newBuilder().setNumber(request.getNumber() + 1).build();
  observer.onNext(response);
  observer.onCompleted();
}
