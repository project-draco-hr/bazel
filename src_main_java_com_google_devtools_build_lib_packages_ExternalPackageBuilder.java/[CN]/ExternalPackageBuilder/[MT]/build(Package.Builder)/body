{
  for (  Rule rule : repositoryMap.values()) {
    try {
      pkg.addRule(rule);
    }
 catch (    Package.NameConflictException e) {
      throw new IllegalStateException("Got a name conflict for " + rule + ", which can't happen: "+ e.getMessage(),e);
    }
  }
}
