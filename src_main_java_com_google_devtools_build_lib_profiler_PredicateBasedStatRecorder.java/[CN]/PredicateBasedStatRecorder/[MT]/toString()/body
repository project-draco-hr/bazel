{
  StringBuilder sb=new StringBuilder();
  for (  StatRecorder recorder : recorders) {
    if (recorder.isEmpty()) {
      continue;
    }
    sb.append(recorder);
    sb.append("\n");
  }
  return sb.toString();
}
