{
  Version oldGraphVersion=graphVersion;
  graphVersion=graphVersion.next();
  return new ParallelEvaluator(graph,oldGraphVersion,builders,reporter,new MemoizingEvaluator.EmittedEventState(),storedEventFilter,keepGoing,150,revalidationReceiver,new DirtyKeyTrackerImpl());
}
