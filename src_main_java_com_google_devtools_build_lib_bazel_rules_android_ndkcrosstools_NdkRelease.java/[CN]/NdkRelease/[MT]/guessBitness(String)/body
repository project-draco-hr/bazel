{
  NdkRelease baseRelease=create(baseReleaseString);
  boolean is64Bit=(CPU.getCurrent() == CPU.X86_64);
  return new NdkRelease(baseRelease.rawRelease + (is64Bit ? " (64-bit)" : ""),baseRelease.isValid,baseRelease.release,baseRelease.releaseCandidate,is64Bit);
}
