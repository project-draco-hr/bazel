{
  if (this == other) {
    return true;
  }
  if (!(other instanceof Path)) {
    return false;
  }
  Path otherPath=(Path)other;
  return fileSystem.equals(otherPath.fileSystem) && name.equals(otherPath.name) && Objects.equals(parent,otherPath.parent);
}
