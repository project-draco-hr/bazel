{
  if (children != null) {
    for (    Reference<Path> childRef : children.values()) {
      Path child=childRef.get();
      if (child != null) {
        function.apply(child);
      }
    }
  }
}
