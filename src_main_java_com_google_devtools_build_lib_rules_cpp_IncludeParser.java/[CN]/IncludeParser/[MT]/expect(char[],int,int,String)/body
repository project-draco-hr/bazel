{
  int si=0;
  int expectedLen=expected.length();
  while (pos < end) {
    if (si == expectedLen) {
      return pos;
    }
    if (chars[pos++] != expected.charAt(si++)) {
      return -1;
    }
  }
  return -1;
}
