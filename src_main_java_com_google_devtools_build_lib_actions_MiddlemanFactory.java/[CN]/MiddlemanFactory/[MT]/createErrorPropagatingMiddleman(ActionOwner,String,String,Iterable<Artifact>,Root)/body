{
  Preconditions.checkArgument(inputs != null);
  Preconditions.checkArgument(!Iterables.isEmpty(inputs));
  return createMiddleman(owner,middlemanName,purpose,inputs,middlemanDir,MiddlemanType.ERROR_PROPAGATING_MIDDLEMAN).getFirst();
}
