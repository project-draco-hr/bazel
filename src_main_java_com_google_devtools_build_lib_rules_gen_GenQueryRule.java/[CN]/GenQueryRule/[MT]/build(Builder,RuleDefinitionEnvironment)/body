{
  return builder.add(attr("scope",LABEL_LIST).mandatory().legacyAllowAnyFileType()).add(attr("strict",BOOLEAN).value(true)).add(attr("expression",STRING).mandatory()).add(attr("opts",STRING_LIST)).build();
}
