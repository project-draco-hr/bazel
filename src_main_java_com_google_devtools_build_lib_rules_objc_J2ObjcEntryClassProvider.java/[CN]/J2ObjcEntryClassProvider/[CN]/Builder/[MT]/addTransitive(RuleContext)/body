{
  if (ruleContext.attributes().has("deps",BuildType.LABEL_LIST)) {
    for (    J2ObjcEntryClassProvider provider : ruleContext.getPrerequisites("deps",Mode.TARGET,J2ObjcEntryClassProvider.class)) {
      addTransitive(provider);
    }
  }
  return this;
}
