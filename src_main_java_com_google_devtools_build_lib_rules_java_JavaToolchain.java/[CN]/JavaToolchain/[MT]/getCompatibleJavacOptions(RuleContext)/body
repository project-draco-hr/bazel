{
  ImmutableMap.Builder<String,ImmutableList<String>> result=ImmutableMap.builder();
  for (  Map.Entry<String,List<String>> entry : ruleContext.attributes().get("compatible_javacopts",Type.STRING_LIST_DICT).entrySet()) {
    result.put(entry.getKey(),ImmutableList.copyOf(JavaHelper.tokenizeJavaOptions(entry.getValue())));
  }
  return result.build();
}
