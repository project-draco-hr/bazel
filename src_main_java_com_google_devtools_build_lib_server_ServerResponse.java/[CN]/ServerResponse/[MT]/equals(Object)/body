{
  if (other == null || !(other instanceof ServerResponse))   return false;
  ServerResponse otherResponse=(ServerResponse)other;
  return exitStatus == otherResponse.exitStatus && errorMessage.equals(otherResponse.errorMessage);
}
