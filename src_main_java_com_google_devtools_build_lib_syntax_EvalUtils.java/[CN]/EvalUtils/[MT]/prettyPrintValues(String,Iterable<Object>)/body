{
  return Joiner.on(separator).join(Iterables.transform(o,new com.google.common.base.Function<Object,String>(){
    @Override public String apply(    Object input){
      return prettyPrintValue(input);
    }
  }
));
}
