{
  if (o instanceof Map<?,?> || o instanceof Function || o instanceof FilesetEntry|| o instanceof GlobList<?>) {
    return false;
  }
 else   if (o instanceof List<?>) {
    return isTuple((List<?>)o);
  }
 else {
    return true;
  }
}
