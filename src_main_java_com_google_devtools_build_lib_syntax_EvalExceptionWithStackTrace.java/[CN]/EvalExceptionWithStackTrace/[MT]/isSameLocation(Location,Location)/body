{
  try {
    return Objects.equals(first.getPath(),second.getPath()) && Objects.equals(first.getStartOffset(),second.getStartOffset());
  }
 catch (  NullPointerException ex) {
    return first == second;
  }
}
