{
  if (element.getCause() == null) {
    return "";
  }
  Location location=getDisplayLocation(element);
  return String.format("\tFile \"%s\", line %d, in %s%n\t\t%s",printPath(location.getPath()),location.getStartLineAndColumn().getLine(),element.getLabel(),element.getCause().getLabel());
}
