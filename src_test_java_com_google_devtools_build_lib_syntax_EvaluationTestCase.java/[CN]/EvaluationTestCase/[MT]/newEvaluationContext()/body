{
  if (testMode == null) {
    throw new IllegalArgumentException("TestMode is null. Please set a Testmode via setMode() or set the " + "evaluatenContext manually by overriding newEvaluationContext()");
  }
  return testMode.createContext(getEventHandler(),factory.getEnvironment());
}
