{
  PackageFunction.SkylarkImportResult importResult;
  importResult=PackageFunction.fetchImportsFromBuildFile(repoWorkspace,PackageIdentifier.createInDefaultRepo("external"),parser.getBuildFileAST(),skyEnvironment,null);
  if (importResult == null) {
    return false;
  }
  parser.setImportedExtensions(importResult.importMap);
  return true;
}
