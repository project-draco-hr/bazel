{
  PackageFunction.SkylarkImportResult importResult;
  importResult=PackageFunction.fetchImportsFromBuildFile(repoWorkspace,Label.EXTERNAL_PACKAGE_IDENTIFIER,parser.getBuildFileAST(),skyEnvironment,null);
  if (importResult == null) {
    return false;
  }
  parser.setImportedExtensions(importResult.importMap);
  return true;
}
