{
  Set<Label> labels=new LinkedHashSet<Label>();
  for (  Collection<Label> labelCollection : computeAspectDependenciesWithAttributes(target).values()) {
    labels.addAll(labelCollection);
  }
  return labels;
}
