{
  this.buffer=input.getContent();
  this.pos=0;
  this.parsePython=parsePython;
  this.eventHandler=eventHandler;
  this.locationInfo=new LocationInfo(input.getPath(),LineNumberTable.create(buffer,input.getPath()));
  indentStack.push(0);
  tokenize();
}
