{
  return ImmutableSet.copyOf((Iterable<RootedPath>)(Iterable)Iterables.transform(Iterables.filter(graph.getValues().keySet(),SkyFunctionName.functionIs(SkyFunctions.FILE_STATE)),SkyKey.NODE_NAME));
}
