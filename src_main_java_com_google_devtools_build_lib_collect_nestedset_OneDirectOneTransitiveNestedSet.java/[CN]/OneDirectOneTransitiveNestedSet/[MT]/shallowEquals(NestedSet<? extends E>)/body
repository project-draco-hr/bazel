{
  if (this == other) {
    return true;
  }
  return other != null && getOrder().equals(other.getOrder()) && other instanceof OneDirectOneTransitiveNestedSet && direct.equals(((OneDirectOneTransitiveNestedSet)other).direct) && transitive == ((OneDirectOneTransitiveNestedSet)other).transitive;
}
