{
  if (!type.isInstance(object)) {
    throw new EvalException(null,Printer.format("Illegal argument: expected type %r %sbut got type %s instead",type,description == null ? "" : String.format("for %s ",description),EvalUtils.getDataTypeName(object)));
  }
}
