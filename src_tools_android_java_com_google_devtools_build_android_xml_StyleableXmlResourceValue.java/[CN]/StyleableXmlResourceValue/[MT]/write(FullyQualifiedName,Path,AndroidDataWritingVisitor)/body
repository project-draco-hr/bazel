{
  mergedDataWriter.writeToValuesXml(key,FluentIterable.from(ImmutableList.of(String.format("<!-- %s -->",source),String.format("<declare-styleable name='%s'>",key.name()))).append(FluentIterable.from(attrs.keySet()).transform(ITEM_TO_ATTR)).append("</declare-styleable>"));
}
