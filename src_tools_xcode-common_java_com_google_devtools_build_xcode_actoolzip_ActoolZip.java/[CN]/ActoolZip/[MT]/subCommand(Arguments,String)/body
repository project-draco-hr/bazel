{
  return new ImmutableList.Builder<String>().add(args.subtoolCmd()).add("--compile").add(outputDirectory).addAll(Iterables.transform(args.subtoolExtraArgs(),Wrappers.CANONICALIZE_IF_PATH)).build();
}
