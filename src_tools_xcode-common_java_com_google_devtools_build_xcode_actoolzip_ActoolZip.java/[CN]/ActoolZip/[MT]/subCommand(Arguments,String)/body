{
  return new ImmutableList.Builder<String>().add(args.subtoolCmd()).add("--output-format").add("human-readable-text").add("--notices").add("--warnings").add("--compress-pngs").add("--compile").add(outputDirectory).addAll(Iterables.transform(args.subtoolExtraArgs(),Wrappers.CANONICALIZE_IF_PATH)).build();
}
