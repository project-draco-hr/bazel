{
  return builder.setImplicitOutputsFunction(XcodeSupport.PBXPROJ).override(builder.copy("deps").nonEmpty().allowedRuleClasses("objc_binary","ios_test","objc_bundle_library","objc_import","objc_library")).override(attr("testonly",BOOLEAN).nonconfigurable("Must support test deps.").value(true)).add(attr("$xcodegen",LABEL).cfg(HOST).exec().value(env.getLabel("//tools/objc:xcodegen"))).build();
}
