{
  int apiVersion=0;
  try {
    apiVersion=Integer.parseInt(junitApiVersion);
  }
 catch (  NumberFormatException e) {
  }
  Preconditions.checkState(apiVersion == 1,"Unsupported JUnit Runner API version %s=%s (must be \"1\")",JUNIT_API_VERSION_PROPERTY,junitApiVersion);
  return apiVersion;
}
