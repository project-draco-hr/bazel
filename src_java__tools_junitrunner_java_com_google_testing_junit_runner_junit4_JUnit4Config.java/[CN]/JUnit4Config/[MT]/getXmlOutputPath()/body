{
  if (!xmlOutputPath.isPresent()) {
    String envXmlOutputPath=System.getenv(XML_OUTPUT_FILE_ENV_VAR);
    return envXmlOutputPath == null ? Optional.<Path>absent() : Optional.of(FileSystems.getDefault().getPath(envXmlOutputPath));
  }
  return xmlOutputPath;
}
