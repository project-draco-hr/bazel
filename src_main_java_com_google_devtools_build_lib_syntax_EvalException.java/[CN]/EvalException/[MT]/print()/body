{
  return this.getClass().getName() + (getLocation() == null ? "" : " at " + getLocation()) + ": "+ (message == null ? "" : message + "\n")+ (dueToIncompleteAST ? "due to incomplete AST\n" : "")+ (getCause() != null && getCause().getMessage() != null ? getCause().getMessage() : "");
}
