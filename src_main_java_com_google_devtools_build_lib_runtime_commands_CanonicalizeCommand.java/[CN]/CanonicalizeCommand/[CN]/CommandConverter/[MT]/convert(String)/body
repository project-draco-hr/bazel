{
  if (input.equals("build")) {
    return input;
  }
 else   if (input.equals("test")) {
    return input;
  }
  throw new OptionsParsingException("Not a valid command: '" + input + "' (should be "+ getTypeDescription()+ ")");
}
