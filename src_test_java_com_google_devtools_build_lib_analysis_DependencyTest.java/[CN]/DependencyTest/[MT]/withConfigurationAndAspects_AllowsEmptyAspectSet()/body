{
  update();
  Dependency dep=Dependency.withConfigurationAndAspects(Label.parseAbsolute("//a"),getTargetConfiguration(),ImmutableSet.<Aspect>of());
  assertThat(dep.getAspects()).isEmpty();
  assertThat(dep.getAspectConfigurations()).isEmpty();
}
