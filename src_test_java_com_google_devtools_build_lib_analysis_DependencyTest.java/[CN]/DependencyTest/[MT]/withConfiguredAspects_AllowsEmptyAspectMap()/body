{
  update();
  Dependency dep=Dependency.withConfiguredAspects(Label.parseAbsolute("//a"),getTargetConfiguration(),ImmutableMap.<Aspect,BuildConfiguration>of());
  assertThat(dep.getAspects()).isEmpty();
  assertThat(dep.getAspectConfigurations()).isEmpty();
}
