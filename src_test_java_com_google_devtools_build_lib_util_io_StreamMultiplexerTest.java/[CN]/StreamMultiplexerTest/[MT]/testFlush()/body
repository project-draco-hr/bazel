{
  out.write(getLatin("Don't forget to flush!"));
  assertArrayEquals(new byte[0],multiplexed.toByteArray());
  out.flush();
  assertEquals(joinLines("@1@","Don't forget to flush!",""),getLatin(multiplexed.toByteArray()));
}
