{
  return builder.add(attr("url",STRING).mandatory()).add(attr("sha256",STRING)).add(attr("build_file",STRING)).add(attr("build_file_content",STRING)).add(attr("type",STRING)).add(attr("strip_prefix",STRING)).setWorkspaceOnly().build();
}
