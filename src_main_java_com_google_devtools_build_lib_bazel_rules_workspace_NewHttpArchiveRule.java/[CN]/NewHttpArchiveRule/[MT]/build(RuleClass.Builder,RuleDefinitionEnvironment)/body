{
  return builder.add(attr("url",STRING).mandatory()).add(attr("sha256",STRING)).add(attr("build_file",STRING).mandatory()).add(attr("type",STRING)).add(attr("strip_prefix",STRING)).setWorkspaceOnly().build();
}
