{
  Map<Artifact,FileArtifactValue> result=new HashMap<>();
  for (  Map.Entry<SkyKey,SkyValue> entry : map.entrySet()) {
    result.put(ArtifactValue.artifact(entry.getKey()),(FileArtifactValue)entry.getValue());
  }
  return result;
}
