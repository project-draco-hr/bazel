{
  if (this == other) {
    return true;
  }
  return other != null && getOrder().equals(other.getOrder()) && other instanceof ManyDirectManyTransitive && Arrays.equals(directs,((ManyDirectManyTransitive)other).directs) && Arrays.equals(transitives,((ManyDirectManyTransitive)other).transitives);
}
