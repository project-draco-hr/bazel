{
  return builder.add(attr("tools",LABEL_LIST).cfg(HOST).allowedFileTypes().exec()).add(attr("out_templates",STRING_LIST)).add(attr("cmd",STRING).mandatory()).add(attr("requires_action_output",BOOLEAN)).removeAttribute("deps").removeAttribute(":action_listener").build();
}
