{
  if (artifact == null) {
    return true;
  }
  PathFragment root=artifact.getRoot().getPath().asFragment();
  return root.segmentCount() < 2 || !root.getSegment(root.segmentCount() - 2).equals(Label.EXTERNAL_PACKAGE_NAME.getPathString());
}
