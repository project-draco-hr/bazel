{
  try {
    return Maps.transformValues(targetPatternEvaluator.preloadTargetPatterns(eventHandler,patterns,keepGoing),RESOLVED_TARGETS_TO_TARGETS);
  }
 catch (  InterruptedException e) {
    throw new TargetParsingException("interrupted");
  }
}
