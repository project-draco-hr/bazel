{
  if (buildConfiguration != null) {
    return ruleContext.getPrerequisitesByConfiguration("deps",Mode.SPLIT,FileProvider.class).get(buildConfiguration);
  }
  return ruleContext.getPrerequisites("deps",Mode.TARGET,FileProvider.class);
}
