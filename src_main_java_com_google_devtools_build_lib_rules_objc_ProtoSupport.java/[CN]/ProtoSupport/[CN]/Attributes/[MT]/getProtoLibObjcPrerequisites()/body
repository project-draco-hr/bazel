{
  if (buildConfiguration != null) {
    return ruleContext.getPrerequisitesByConfiguration(ObjcRuleClasses.PROTO_LIB_ATTR,Mode.SPLIT,ObjcProvider.class).get(buildConfiguration);
  }
  return ruleContext.getPrerequisites(ObjcRuleClasses.PROTO_LIB_ATTR,Mode.TARGET,ObjcProvider.class);
}
