{
  if (buildConfiguration != null) {
    return ruleContext.getPrerequisitesByConfiguration("deps",Mode.SPLIT,ObjcProvider.class).get(buildConfiguration);
  }
  return ruleContext.getPrerequisites("deps",Mode.TARGET,ObjcProvider.class);
}
