{
  return new SkyKey(SkyFunctions.TARGET_PATTERN,pattern.startsWith("-") ? new TargetPattern(pattern.substring(1),FilteringPolicies.NO_FILTER,true,offset) : new TargetPattern(pattern,policy,false,offset));
}
