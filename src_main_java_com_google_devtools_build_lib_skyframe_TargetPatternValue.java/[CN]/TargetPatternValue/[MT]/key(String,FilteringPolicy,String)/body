{
  return new SkyKey(SkyFunctions.TARGET_PATTERN,pattern.startsWith("-") ? new TargetPatternKey(pattern.substring(1),FilteringPolicies.NO_FILTER,true,offset) : new TargetPatternKey(pattern,policy,false,offset));
}
