{
  mergedDataWriter.writeToValuesXml(key,FluentIterable.from(ImmutableList.of(String.format("<!-- %s -->",source),arrayType.openTag(key,attributes))).append(FluentIterable.from(values).transform(ITEM_TO_XML)).append(arrayType.closeTag()));
}
