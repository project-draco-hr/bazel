{
  List<String> values=new ArrayList<>();
  for (XMLEvent element=eventReader.nextTag(); !XmlResourceValues.isEndTag(element,start.getName()); element=eventReader.nextTag()) {
    if (XmlResourceValues.isItem(element)) {
      values.add(eventReader.getElementText());
    }
  }
  return of(ArrayType.fromTagName(start),values);
}
