{
  if (this == o) {
    return true;
  }
  if (!(o instanceof TransitiveTraversalValue)) {
    return false;
  }
  TransitiveTraversalValue that=(TransitiveTraversalValue)o;
  return this.canHaveAnyProvider == that.canHaveAnyProvider && Objects.equals(this.firstErrorMessage,that.firstErrorMessage) && this.providers.equals(that.providers);
}
