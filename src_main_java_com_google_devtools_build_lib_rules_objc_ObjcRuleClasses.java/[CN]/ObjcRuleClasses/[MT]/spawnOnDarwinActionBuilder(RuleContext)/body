{
  AppleConfiguration appleConfiguration=ruleContext.getFragment(AppleConfiguration.class);
  return new SpawnAction.Builder().setEnvironment(appleConfiguration.getEnvironmentForIosAction()).setExecutionInfo(ImmutableMap.of(ExecutionRequirements.REQUIRES_DARWIN,""));
}
