{
  ObjcConfiguration objcConfiguration=objcConfiguration(ruleContext);
  return new SpawnAction.Builder().setEnvironment(objcConfiguration.getEnvironmentForDarwin()).setExecutionInfo(ImmutableMap.of(ExecutionRequirements.REQUIRES_DARWIN,""));
}
