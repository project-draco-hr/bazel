{
  return builder.add(attr("$xcodegen",LABEL).cfg(HOST).exec().value(env.getLabel(env.getToolsRepository() + "//tools/objc:xcodegen"))).add(attr("$dummy_source",LABEL).value(env.getLabel(env.getToolsRepository() + "//tools/objc:dummy.c"))).build();
}
