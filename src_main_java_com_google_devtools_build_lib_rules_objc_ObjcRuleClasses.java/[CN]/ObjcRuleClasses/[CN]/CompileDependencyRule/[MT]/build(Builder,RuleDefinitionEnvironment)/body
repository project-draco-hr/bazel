{
  return builder.add(attr("hdrs",LABEL_LIST).direct_compile_time_input().allowedFileTypes(HDRS_TYPE)).add(attr("bridging_header",Type.LABEL).direct_compile_time_input().allowedFileTypes(HDRS_TYPE)).add(attr("includes",Type.STRING_LIST)).add(attr("sdk_includes",Type.STRING_LIST)).build();
}
