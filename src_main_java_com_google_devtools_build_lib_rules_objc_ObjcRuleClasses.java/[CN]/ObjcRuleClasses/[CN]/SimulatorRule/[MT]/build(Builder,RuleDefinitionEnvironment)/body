{
  return builder.add(attr("$iossim",LABEL).cfg(HOST).exec().value(env.getLabel("//third_party/iossim:iossim"))).add(attr("$std_redirect_dylib",LABEL).cfg(HOST).exec().value(env.getLabel(Constants.TOOLS_REPOSITORY + "//tools/objc:StdRedirect.dylib"))).build();
}
