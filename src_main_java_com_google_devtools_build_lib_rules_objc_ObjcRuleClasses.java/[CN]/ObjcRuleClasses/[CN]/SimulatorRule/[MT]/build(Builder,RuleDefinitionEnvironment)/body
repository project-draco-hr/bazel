{
  return builder.add(attr("$iossim",LABEL).cfg(HOST).exec().value(env.getLabel(env.getToolsRepository() + "//third_party/iossim:iossim"))).add(attr("$std_redirect_dylib",LABEL).cfg(HOST).exec().value(env.getLabel(env.getToolsRepository() + "//tools/objc:StdRedirect.dylib"))).build();
}
