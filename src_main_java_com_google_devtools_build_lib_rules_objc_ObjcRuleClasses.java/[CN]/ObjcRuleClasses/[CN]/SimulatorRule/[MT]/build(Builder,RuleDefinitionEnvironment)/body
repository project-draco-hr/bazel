{
  return builder.add(attr("$iossim",LABEL).cfg(HOST).exec().value(env.getToolsLabel("//third_party/iossim:iossim"))).add(attr("$std_redirect_dylib",LABEL).cfg(HOST).exec().value(env.getToolsLabel("//tools/objc:StdRedirect.dylib"))).build();
}
