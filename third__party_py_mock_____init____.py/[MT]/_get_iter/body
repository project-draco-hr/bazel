def _get_iter(self):

    def __iter__():
        ret_val = self.__iter__._mock_return_value
        if (ret_val is DEFAULT):
            return iter([])
        return iter(ret_val)
    return __iter__
