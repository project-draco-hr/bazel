{
  events.setFailFast(false);
  assertGlobFails("glob(1, exclude=2)","Method glob(include: sequence of strings, *, exclude: sequence of strings, " + "exclude_directories: int) is not applicable for arguments (int, int, int): " + "'include' is int, but should be sequence");
}
