{
  return iterable.append(FluentIterable.from(values.entrySet()).transform(new Function<Entry<String,String>,String>(){
    @Nullable @Override public String apply(    Entry<String,String> input){
      return String.format("<flag name='%s' value='%s'/>",input.getKey(),input.getValue());
    }
  }
));
}
