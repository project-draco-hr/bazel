{
  ConfigurationEnvironment env=new ConfigurationEnvironment.TargetProviderEnvironment(skyframeExecutor.getPackageManager(),reporter,directories);
  return loader.create(env,createBuildOptionsForTest(args));
}
