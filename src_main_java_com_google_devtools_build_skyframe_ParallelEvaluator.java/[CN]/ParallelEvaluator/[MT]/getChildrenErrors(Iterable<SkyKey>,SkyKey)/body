{
  List<ErrorInfo> allErrors=new ArrayList<>();
  for (  SkyKey child : children) {
    ErrorInfo errorInfo=getErrorMaybe(child,child.equals(unfinishedChild));
    if (errorInfo != null) {
      allErrors.add(errorInfo);
    }
  }
  return allErrors;
}
