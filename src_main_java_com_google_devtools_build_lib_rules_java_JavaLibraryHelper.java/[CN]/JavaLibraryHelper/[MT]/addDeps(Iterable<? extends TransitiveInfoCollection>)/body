{
  for (  TransitiveInfoCollection dep : deps) {
    Preconditions.checkArgument(dep.getConfiguration() == null || dep.getConfiguration().equals(configuration));
    this.deps.add(dep);
  }
  return this;
}
