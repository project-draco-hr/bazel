{
  RawAttributeMapper rawMapper=RawAttributeMapper.of(setupGenRule());
  assertEquals(Type.LABEL_LIST,rawMapper.getAttributeType("data"));
  assertEquals(Type.LABEL_LIST,rawMapper.getAttributeType("srcs"));
}
