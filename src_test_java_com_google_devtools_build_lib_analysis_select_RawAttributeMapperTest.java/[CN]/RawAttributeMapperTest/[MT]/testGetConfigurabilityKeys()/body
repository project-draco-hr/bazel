{
  RawAttributeMapper rawMapper=RawAttributeMapper.of(setupGenRule());
  assertSameContents(ImmutableSet.of(Label.parseAbsolute("//conditions:a"),Label.parseAbsolute("//conditions:b"),Label.parseAbsolute("//conditions:default")),rawMapper.getConfigurabilityKeys("srcs",Type.LABEL_LIST));
  assertThat(rawMapper.getConfigurabilityKeys("data",Type.LABEL_LIST)).isEmpty();
}
