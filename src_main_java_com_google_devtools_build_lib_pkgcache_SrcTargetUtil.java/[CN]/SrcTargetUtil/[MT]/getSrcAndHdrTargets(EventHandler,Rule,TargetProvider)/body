{
  ImmutableSet<String> srcAndHdrAttributes=ImmutableSet.<String>builder().addAll(SOURCE_ATTRIBUTES).addAll(HEADER_ATTRIBUTES).build();
  return getTargets(eventHandler,rule,srcAndHdrAttributes,Sets.newHashSet(rule),provider);
}
