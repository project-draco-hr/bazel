{
  ImmutableSet<String> srcAndHdrAttributes=ImmutableSet.<String>builder().addAll(SOURCE_ATTRIBUTES).add(HEADER_ATTRIBUTE).add(TEXTUAL_HEADER_ATTRIBUTE).build();
  return getTargets(eventHandler,rule,srcAndHdrAttributes,Sets.newHashSet(rule.getLabel()),provider);
}
