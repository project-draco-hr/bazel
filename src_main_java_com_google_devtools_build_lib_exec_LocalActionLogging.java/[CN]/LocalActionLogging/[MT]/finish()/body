{
  Preconditions.checkState(isRunning,action);
  isRunning=false;
  LOG.info("Finished running " + action.prettyPrint() + " in "+ TimeUnit.NANOSECONDS.toMillis(BlazeClock.nanoTime() - startTime)+ "ms");
}
