{
  if (this == other) {
    return true;
  }
  if (other == null) {
    return false;
  }
  return getOrder().equals(other.getOrder()) && other instanceof OnlyDirectsNestedSet && Arrays.equals(directDeps,((OnlyDirectsNestedSet)other).directDeps);
}
