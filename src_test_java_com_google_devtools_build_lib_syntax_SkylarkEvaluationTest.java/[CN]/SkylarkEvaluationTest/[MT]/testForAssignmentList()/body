{
  List<Statement> input=parseFileForSkylark("def foo():\n" + "  d = ['a', 'b', 'c']\n" + "  s = ''\n"+ "  for i in d:\n"+ "    s = s + i\n"+ "    d = ['d', 'e', 'f']\n"+ "  return s\n"+ "s = foo()\n");
  exec(input,env);
  assertEquals("abc",env.lookup("s"));
}
