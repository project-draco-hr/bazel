{
  env.update("mock",new MockSubClass());
  List<Statement> input=parseFileForSkylark("b = mock.is_empty_class_not_annotated('a')",MOCK_TYPES);
  checkEvalError(input,env,"No matching method found for is_empty_class_not_annotated(string) in MockSubClass");
}
