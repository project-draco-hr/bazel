{
  List<Statement> input=parseFileForSkylark("def foo():\n" + "  s = ''\n" + "  for i in ['a', 'b']:\n"+ "    for i in ['c', 'd']: s = s + i\n"+ "  return s\n"+ "s = foo()");
  exec(input,env);
  assertEquals("cdcd",env.lookup("s"));
}
