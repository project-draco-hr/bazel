{
  List<Statement> input=parseFileForSkylark("def foo():\n" + "  d = {1: 'a', 2: 'b', 3: 'c'}\n" + "  s = ''\n"+ "  for i in d: s = s + d[i]\n"+ "  return s\n"+ "s = foo()");
  exec(input,env);
  assertEquals("abc",env.lookup("s"));
}
