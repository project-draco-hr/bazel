{
  exec(parseFileForSkylark("a = None\n" + "b = True\n" + "c = False"),env);
  assertSame(Environment.NONE,env.lookup("a"));
  assertTrue((Boolean)env.lookup("b"));
  assertFalse((Boolean)env.lookup("c"));
}
