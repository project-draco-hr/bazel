{
  checkEvalError("in operator only works on strings if the left operand is also a string","1 in '123'");
  checkEvalError("ERROR 1:1: operand 'in' only works on " + "strings, dictionaries, lists, sets or tuples, not on a(n) int","'a' in 1");
}
