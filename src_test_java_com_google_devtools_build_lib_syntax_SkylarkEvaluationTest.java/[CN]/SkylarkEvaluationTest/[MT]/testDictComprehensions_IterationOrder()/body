{
  List<Statement> input=parseFileForSkylark("def foo():\n" + "  d = {x : x for x in ['c', 'a', 'b']}\n" + "  s = ''\n"+ "  for a in d:\n"+ "    s += a\n"+ "  return s\n"+ "s = foo()");
  exec(input,env);
  assertEquals("abc",env.lookup("s"));
}
