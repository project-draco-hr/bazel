{
  if (cppConfiguration.isLipoContextCollector()) {
    return NestedSetBuilder.emptySet(Order.STABLE_ORDER);
  }
  return NestedSetBuilder.wrap(Order.STABLE_ORDER,compilationOutputs.getObjectFiles(CppHelper.usePic(ruleContext,false)));
}
