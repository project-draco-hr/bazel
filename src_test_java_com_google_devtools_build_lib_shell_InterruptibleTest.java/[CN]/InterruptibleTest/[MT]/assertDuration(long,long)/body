{
  long after=System.nanoTime();
  long millis=(after - before) / 1000000;
  if (millis < minMillis || millis > maxMillis) {
    fail("Duration " + millis + "ms was not in range "+ minMillis+ "-"+ maxMillis+ "ms");
  }
}
