{
  Object input=ImmutableMap.of("foo",Arrays.asList("//foo:.."),"wiz",Arrays.asList(":bang"));
  try {
    Type.LABEL_LIST_DICT.convert(input,"baz",currentRule);
    fail();
  }
 catch (  Type.ConversionException e) {
    assertThat(e).hasMessage("invalid label '//foo:..' in element 0 of dict value element: " + "invalid target name '..': " + "target names may not contain up-level references '..'");
  }
}
