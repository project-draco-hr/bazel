{
  Object input=ImmutableMap.of("foo","bar",Arrays.asList("foo","bar"),Arrays.<Object>asList("wiz","bang"));
  try {
    Type.STRING_DICT_UNARY.convert(input,null);
    fail();
  }
 catch (  Type.ConversionException e) {
    assertThat(e).hasMessage("expected value of type 'string' for dict key element, but got " + "'[\"foo\", \"bar\"]' (List)");
  }
}
