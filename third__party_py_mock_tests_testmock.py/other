from tests.support import callable, unittest2, inPy3k, is_instance, next
import copy
import pickle
import sys
import mock
from mock import call, DEFAULT, patch, sentinel, MagicMock, Mock, NonCallableMock, NonCallableMagicMock, _CallList, create_autospec
try:
    unicode
except NameError:
    unicode = str
if (__name__ == '__main__'):
    unittest2.main()
