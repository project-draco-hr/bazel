{
  OptionFileExpander expander=new OptionFileExpander(new StoredOptionFileProvider());
  assertEquals(Arrays.asList("--some","option","list"),expander.expandArguments(Arrays.asList("--some","option","list")));
}
