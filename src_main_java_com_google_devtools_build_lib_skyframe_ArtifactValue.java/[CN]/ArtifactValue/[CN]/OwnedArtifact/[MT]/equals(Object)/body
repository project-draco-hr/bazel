{
  if (this == that) {
    return true;
  }
  if (!(that instanceof OwnedArtifact)) {
    return false;
  }
  OwnedArtifact thatOwnedArtifact=((OwnedArtifact)that);
  Artifact thatArtifact=thatOwnedArtifact.artifact;
  return artifact.equals(thatArtifact) && artifact.getArtifactOwner().equals(thatArtifact.getArtifactOwner()) && isMandatory == thatOwnedArtifact.isMandatory;
}
