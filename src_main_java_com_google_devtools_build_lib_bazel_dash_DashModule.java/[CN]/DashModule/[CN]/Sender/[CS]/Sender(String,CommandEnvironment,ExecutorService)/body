{
  this.url=url;
  this.buildId=env.getCommandId().toString();
  this.reporter=env.getReporter();
  this.executorService=executorService;
  env.getReporter().handle(Event.info("Results are being streamed to " + url + "/result/"+ buildId));
}
