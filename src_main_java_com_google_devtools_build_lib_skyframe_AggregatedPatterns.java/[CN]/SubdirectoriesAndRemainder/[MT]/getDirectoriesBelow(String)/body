{
  if (directoryWithoutTrailingSlash.length() == 0) {
    return orderedPatternsByDirectory.keySet();
  }
  String afterKey=getKeyAfterAllPathsBelowDirectoryWithoutTrailingSlash(directoryWithoutTrailingSlash);
  return orderedPatternsByDirectory.subMap(getDirectoryKey(directoryWithoutTrailingSlash),afterKey).keySet();
}
