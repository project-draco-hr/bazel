{
  checkWarning("uplevel","lib","in includes attribute of cc_library rule //uplevel:lib: '../bar' resolves to 'bar' not " + "below the relative path of its package 'uplevel'. This will be an error in the " + "future","cc_library(name = 'lib',","           srcs = ['foo.cc'],","           includes = ['../bar'])");
}
