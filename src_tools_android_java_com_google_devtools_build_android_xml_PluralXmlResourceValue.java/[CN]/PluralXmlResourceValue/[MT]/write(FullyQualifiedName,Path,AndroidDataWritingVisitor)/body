{
  mergedDataWriter.writeToValuesXml(key,FluentIterable.of(String.format("<!-- %s -->",source),String.format("<plurals name='%s'>",key.name())).append(FluentIterable.from(values.entrySet()).transform(ENTRY_TO_PLURAL)).append("</plurals>"));
}
