{
  printTimeAxis(chart);
  out.println("</div>");
  heading("Legend",2);
  printLegend(chart.getSortedTypes());
  heading("Statistics",2);
  printStatistics(chart.getStatistics());
  out.println("</body>");
  out.println("</html>");
}
