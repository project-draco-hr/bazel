{
  if (this == other) {
    return true;
  }
  if (other == null) {
    return false;
  }
  return getOrder().equals(other.getOrder()) && other instanceof ImmutableListDirectsNestedSet && directDeps.equals(((ImmutableListDirectsNestedSet)other).directDeps);
}
