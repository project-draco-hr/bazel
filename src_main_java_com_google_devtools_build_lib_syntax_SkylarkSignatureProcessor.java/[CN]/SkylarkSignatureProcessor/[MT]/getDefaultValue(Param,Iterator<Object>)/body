{
  if (iterator != null) {
    return iterator.next();
  }
 else   if (param.defaultValue().isEmpty()) {
    return Environment.NONE;
  }
 else {
    try {
      throw new RuntimeException("Not Implemented Yet!");
    }
 catch (    Exception e) {
      throw new RuntimeException(String.format("Exception while processing @SkylarkSignature.Param %s, default value %s: %s",param.name(),param.defaultValue(),e.getMessage()),e);
    }
  }
}
