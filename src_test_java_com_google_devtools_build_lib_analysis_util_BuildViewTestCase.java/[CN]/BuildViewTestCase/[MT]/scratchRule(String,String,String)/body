{
  String buildFilePathString=packageName + "/BUILD";
  scratch.file(buildFilePathString,lines);
  skyframeExecutor.invalidateFilesUnderPathForTesting(new ModifiedFileSet.Builder().modify(new PathFragment(buildFilePathString)).build(),rootDirectory);
  return (Rule)getTarget("//" + packageName + ":"+ ruleName);
}
