{
  reporter.removeHandler(failFastHandler);
  try {
    getTarget(target);
    fail(String.format("checkLoadingPhaseError(): expected an error with '%s' when loading target '%s'.",expectedErrorMessage,target));
  }
 catch (  Exception e) {
    assertContainsEvent(expectedErrorMessage);
  }
}
