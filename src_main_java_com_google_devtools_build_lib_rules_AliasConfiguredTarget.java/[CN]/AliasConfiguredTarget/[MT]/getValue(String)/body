{
  if (name.equals("label")) {
    return getLabel();
  }
 else   if (name.equals("files")) {
    return SkylarkNestedSet.of(Artifact.class,configuredTarget == null ? NestedSetBuilder.<Artifact>emptySet(Order.STABLE_ORDER) : getProvider(FileProvider.class).getFilesToBuild());
  }
  return configuredTarget == null ? null : configuredTarget.get(name);
}
