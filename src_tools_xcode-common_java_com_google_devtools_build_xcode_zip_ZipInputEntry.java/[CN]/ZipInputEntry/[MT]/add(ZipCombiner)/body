{
  try (InputStream inputStream=Files.newInputStream(source)){
    combiner.addFile(zipPath,ZipCombiner.DOS_EPOCH,inputStream,DEFAULT_DIRECTORY_ENTRY_INFO.withExternalFileAttribute(externalFileAttribute));
  }
 }
