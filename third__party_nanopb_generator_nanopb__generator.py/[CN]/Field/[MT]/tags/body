def tags(self):
    u'Return the #define for the tag number of this field.'
    identifier = (u'%s_%s_tag' % (self.struct_name, self.name))
    return (u'#define %-40s %d\n' % (identifier, self.tag))
