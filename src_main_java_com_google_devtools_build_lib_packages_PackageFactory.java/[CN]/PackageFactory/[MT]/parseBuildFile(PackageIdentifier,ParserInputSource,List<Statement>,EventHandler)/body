{
  LOG.fine("Starting to parse " + packageId);
  BuildFileAST buildFileAST=BuildFileAST.parseBuildFile(in,preludeStatements,eventHandler,false);
  LOG.fine("Finished parsing of " + packageId);
  return buildFileAST;
}
