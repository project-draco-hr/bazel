{
  try {
    return (PackageContext)env.lookup(PKG_CONTEXT);
  }
 catch (  NoSuchVariableException e) {
    throw new EvalException(ast.getLocation(),"This function must be wrapped in a macro and called from a BUILD file");
  }
}
