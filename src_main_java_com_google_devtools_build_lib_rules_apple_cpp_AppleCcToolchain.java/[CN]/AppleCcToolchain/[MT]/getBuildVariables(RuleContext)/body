{
  AppleConfiguration appleConfiguration=ruleContext.getFragment(AppleConfiguration.class);
  return ImmutableMap.of(XCODE_VERSION_KEY,appleConfiguration.getXcodeVersion().or(DEFAULT_XCODE_VERSION).toString());
}
