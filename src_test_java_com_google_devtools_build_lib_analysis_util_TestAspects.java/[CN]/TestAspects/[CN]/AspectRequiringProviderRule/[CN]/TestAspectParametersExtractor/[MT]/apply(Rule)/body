{
  if (rule.isAttrDefined("baz",STRING)) {
    return new AspectParameters.Builder().addAttribute("baz",rule.getAttributeContainer().getAttr("baz").toString()).build();
  }
  return AspectParameters.EMPTY;
}
