{
  Throwable cause=errorInfo.getException();
  if (cause != null) {
    Preconditions.checkState(cause instanceof ConfiguredValueCreationException || cause instanceof ActionConflictException || cause instanceof AspectCreationException|| cause instanceof SkylarkImportFailedException,"%s -> %s",key,errorInfo);
  }
}
