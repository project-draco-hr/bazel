{
  if (countedSize != EvalUtils.size(collection)) {
    throw new EvalException(forStatement.getLocation(),String.format("Cannot modify '%s' during iteration.",((ForStatement)forStatement).collection.toString()));
  }
}
