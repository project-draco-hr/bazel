{
  return builder.add(attr("artifact_id",Type.STRING).mandatory()).add(attr("group_id",Type.STRING).mandatory()).add(attr("version",Type.STRING).mandatory()).add(attr("repositories",Type.STRING_LIST)).add(attr("exclusions",Type.STRING_LIST)).setWorkspaceOnly().build();
}
