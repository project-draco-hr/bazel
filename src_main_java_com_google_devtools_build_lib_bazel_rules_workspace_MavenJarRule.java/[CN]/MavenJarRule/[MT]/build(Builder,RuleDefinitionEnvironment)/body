{
  return builder.add(attr("artifact_id",Type.STRING).mandatory()).add(attr("group_id",Type.STRING).mandatory()).add(attr("version",Type.STRING).mandatory()).add(attr("repository",Type.STRING)).add(attr("repositories",Type.STRING_LIST)).setUndocumented().add(attr("sha1",Type.STRING)).setWorkspaceOnly().build();
}
