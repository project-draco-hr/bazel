{
  return builder.override(attr("deps",LABEL_LIST).allowedRuleClasses("proto_library").allowedFileTypes().aspect(javaProtoAspect)).add(attr(SPEED_PROTO_RUNTIME_ATTR,LABEL).legacyAllowAnyFileType().value(Label.parseAbsoluteUnchecked(SPEED_PROTO_RUNTIME_LABEL))).build();
}
