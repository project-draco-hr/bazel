{
  if (!clazz.isInterface() && clazz.getName().contains(".AutoValue_")) {
    throw new IllegalArgumentException(clazz + " is generated by @AutoValue; use " + clazz.getSuperclass()+ " instead");
  }
}
