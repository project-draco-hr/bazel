{
  if (!clazz.isInterface()) {
    Preconditions.checkArgument(Modifier.isFinal(clazz.getModifiers()),clazz.getName() + " has to be final");
    Preconditions.checkArgument(clazz.isAnnotationPresent(Immutable.class),clazz.getName() + " has to be tagged with @Immutable");
  }
}
