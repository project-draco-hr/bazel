{
  if (!clazz.isInterface()) {
    Preconditions.checkArgument(Modifier.isFinal(clazz.getModifiers()),"%s has to be final",clazz.getName());
    Preconditions.checkArgument(clazz.isAnnotationPresent(Immutable.class),"%s has to be tagged with @Immutable",clazz.getName());
  }
}
