{
  if (!clazz.isInterface()) {
    Preconditions.checkArgument(!clazz.getSimpleName().startsWith("AutoValue_") || clazz.getSuperclass() == null,"%s is generated by @AutoValue - you should use %s instead",clazz.getSimpleName(),clazz.getSuperclass().getSimpleName());
  }
}
