{
  return Iterators.filter(aggregateTaskStatistics.keySet().iterator(),new Predicate<ProfilerTask>(){
    @Override public boolean apply(    ProfilerTask taskType){
      return getTotalDurationNanos(taskType) != 0 && getCount(taskType) != 0;
    }
  }
);
}
