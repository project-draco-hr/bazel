{
  this(Runnables.doNothing(),null,ImmutableList.<ArtifactFile>of(),output,Collections2.transform(Arrays.asList(subOutputs),new Function<String,ArtifactFile>(){
    @Nullable @Override public ArtifactFile apply(    String s){
      return ActionInputHelper.artifactFile(output,s);
    }
  }
));
}
