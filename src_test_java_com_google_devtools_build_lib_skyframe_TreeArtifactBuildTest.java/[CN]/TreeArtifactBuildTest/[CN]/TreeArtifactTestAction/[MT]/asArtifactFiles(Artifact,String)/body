{
  return Lists.transform(Arrays.asList(files),new Function<String,ArtifactFile>(){
    @Nullable @Override public ArtifactFile apply(    String s){
      return ActionInputHelper.artifactFile(parent,s);
    }
  }
);
}
