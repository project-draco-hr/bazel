{
  createMap();
  map.put("foo","bar");
  map.put("baz","bang");
  map.save();
  assertFalse(journalFile.exists());
  map.updateJournal=false;
  map.remove("foo");
  assertEquals(1,map.size());
  assertFalse(journalFile.exists());
  createMap();
  assertEquals(2,map.size());
}
