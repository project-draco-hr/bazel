{
  Object value=deserializeAttributeValue(expectedType,attrPb);
  return new ParsedAttributeValue(attrPb.hasExplicitlySpecified() && attrPb.getExplicitlySpecified(),value,deserializeLocation(context,attrPb.getParseableLocation()));
}
