{
  TypeMirror iteratorType=InternalUtils.typeOf(iteratorCall);
  VariableTree iteratorVariable=treeBuilder.buildVariableDecl(iteratorType,uniqueName("iter"),variableElement.getEnclosingElement(),iteratorCall);
  return iteratorVariable;
}
