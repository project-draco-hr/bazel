{
  ImmutableList.Builder<Artifact> result=ImmutableList.builder();
  for (  Map.Entry<String,Iterable<Artifact>> entry : nativeLibs.entrySet()) {
    String arch=entry.getKey();
    for (    Artifact lib : entry.getValue()) {
      Artifact symlink=AndroidBinary.getDxArtifact(ruleContext,"native_symlinks/" + arch + "/"+ lib.getExecPath().getBaseName());
      ruleContext.registerAction(new SymlinkAction(ruleContext.getActionOwner(),lib,symlink,"Symlinking Android native library for " + ruleContext.getLabel()));
      result.add(symlink);
    }
  }
  return result.build();
}
