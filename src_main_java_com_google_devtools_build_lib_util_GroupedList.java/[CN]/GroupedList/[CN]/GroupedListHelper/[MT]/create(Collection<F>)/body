{
  GroupedListHelper<F> helper=new GroupedListHelper<>();
  helper.addList(elements);
  helper.elements.addAll(elements);
  Preconditions.checkState(helper.elements.size() == elements.size(),"%s %s",helper,elements);
  return helper;
}
