{
  groupedList=GroupedList.remove(groupedList,toRemove);
  int oldSize=size();
  elements.removeAll(toRemove);
  Preconditions.checkState(oldSize == size() + toRemove.size(),"%s %s %s",oldSize,toRemove,this);
}
