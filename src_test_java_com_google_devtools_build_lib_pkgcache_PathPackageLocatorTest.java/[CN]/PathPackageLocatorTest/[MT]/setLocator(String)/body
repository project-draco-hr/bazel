{
  Path nonExistentRoot=scratch.resolve(root);
  this.locator=PathPackageLocator.create(null,Arrays.asList(root),reporter,FileSystemUtils.getWorkingDirectory(scratch.getFileSystem()),FileSystemUtils.getWorkingDirectory(scratch.getFileSystem()));
  return nonExistentRoot;
}
