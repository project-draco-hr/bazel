{
  centralDirectory.setUnsignedInt(offset + 16,(int)crc32);
  centralDirectory.setUnsignedInt(offset + 20,(int)compressedSize);
  centralDirectory.setUnsignedInt(offset + 24,(int)uncompressedSize);
}
