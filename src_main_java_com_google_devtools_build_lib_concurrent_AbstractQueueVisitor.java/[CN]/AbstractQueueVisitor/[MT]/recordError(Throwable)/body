{
  catastrophe=e;
  try {
synchronized (this) {
      if (unhandled == null) {
        unhandled=e;
        exceptionLatch.countDown();
      }
    }
  }
  finally {
    decrementRemainingTasks();
  }
}
