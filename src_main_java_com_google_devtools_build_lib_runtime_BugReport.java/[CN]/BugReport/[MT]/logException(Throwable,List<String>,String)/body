{
  String preamble=exception instanceof OutOfMemoryError ? "Blaze OOMError: " : "Blaze crashed with args: ";
  LoggingUtil.logToRemote(Level.SEVERE,preamble + Joiner.on(' ').join(args),exception,values);
}
