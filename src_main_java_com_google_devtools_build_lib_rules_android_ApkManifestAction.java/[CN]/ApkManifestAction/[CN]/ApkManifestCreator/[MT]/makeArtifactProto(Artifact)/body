{
  byte[] digest=metadataHandler.getMetadata(artifact).digest;
  return ApkManifestOuterClass.Artifact.newBuilder().setExecRootPath(artifact.getExecPathString()).setHash(ByteString.copyFrom(digest)).build();
}
