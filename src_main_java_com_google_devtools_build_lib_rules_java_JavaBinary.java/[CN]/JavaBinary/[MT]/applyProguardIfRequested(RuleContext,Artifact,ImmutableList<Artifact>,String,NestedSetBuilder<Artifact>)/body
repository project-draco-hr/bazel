{
  if (!ruleContext.getRule().getRuleClass().endsWith("_test")) {
    return false;
  }
  ProguardHelper.ProguardOutput output=JavaBinaryProguardHelper.INSTANCE.applyProguardIfRequested(ruleContext,deployJar,bootclasspath,mainClassName);
  if (output == null) {
    return false;
  }
  output.addAllToSet(filesBuilder);
  return true;
}
