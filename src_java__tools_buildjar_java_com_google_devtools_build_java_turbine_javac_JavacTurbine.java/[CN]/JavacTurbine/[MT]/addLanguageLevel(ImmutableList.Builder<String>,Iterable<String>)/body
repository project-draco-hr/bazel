{
  Iterator<String> it=defaultJavacopts.iterator();
  while (it.hasNext()) {
    String curr=it.next();
switch (curr) {
case "-source":
case "-target":
      if (it.hasNext()) {
        javacArgs.add(curr);
        javacArgs.add(it.next());
      }
    break;
default :
  break;
}
}
}
