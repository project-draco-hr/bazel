{
  if (x instanceof Label) {
    return (Label)x;
  }
  try {
    return currentRule.getRelative(STRING.convert(x,what,currentRule));
  }
 catch (  LabelSyntaxException e) {
    throw new ConversionException("invalid label '" + x + "' in "+ what+ ": "+ e.getMessage());
  }
}
