{
  if (value == null) {
    String msg="Illegal tag conversion from null on Attribute " + name + ".";
    throw new IllegalStateException(msg);
  }
  return new ImmutableSet.Builder<String>().add((String)value).build();
}
