{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof ParamFileInfo)) {
    return false;
  }
  ParamFileInfo other=(ParamFileInfo)obj;
  return fileType.equals(other.fileType) && charset.equals(other.charset) && flag.equals(other.flag)&& always == other.always;
}
