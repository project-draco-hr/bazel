{
  if (this == other) {
    return true;
  }
  return other != null && getOrder().equals(other.getOrder()) && other instanceof OnlyOneTransitiveNestedSet && transitive == ((OnlyOneTransitiveNestedSet)other).transitive;
}
