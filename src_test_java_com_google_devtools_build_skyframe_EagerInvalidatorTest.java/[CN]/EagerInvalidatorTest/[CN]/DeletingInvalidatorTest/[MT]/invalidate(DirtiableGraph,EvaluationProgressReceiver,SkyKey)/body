{
  Iterable<SkyKey> diff=ImmutableList.copyOf(keys);
  InvalidatingNodeVisitor invalidatingVisitor=EagerInvalidator.createDeletingVisitorIfNeeded(graph,diff,invalidationReceiver,state,true,dirtyKeyTracker);
  if (invalidatingVisitor != null) {
    visitor.set(invalidatingVisitor);
    invalidatingVisitor.run();
  }
}
