{
  InvalidatingNodeVisitor invalidatingVisitor=EagerInvalidator.createVisitor(true,graph,ImmutableList.copyOf(keys),invalidationReceiver,state,true,dirtyKeyTracker);
  if (invalidatingVisitor != null) {
    visitor.set(invalidatingVisitor);
    invalidatingVisitor.run();
  }
}
