{
  final List<Object> params=new ArrayList<>();
  List<Statement> input=parseFileForSkylark("def func(a):\n" + "  outer_func(a)\n" + "func(1)\n"+ "func(2)",OUTER_FUNC_TYPES);
  createOuterFunction(env,params);
  exec(input,env);
  assertThat(params).containsExactly(1,2).inOrder();
}
