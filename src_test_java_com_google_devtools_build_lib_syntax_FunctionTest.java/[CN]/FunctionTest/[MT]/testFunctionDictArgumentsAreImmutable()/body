{
  MethodLibrary.setupMethodEnvironment(env);
  List<Statement> input=parseFileForSkylark("d = {'a' : 1}\n" + "def func(d):\n" + "  d += {'a' : 2}\n"+ "func(d)");
  exec(input,env);
  assertEquals(ImmutableMap.of("a",1),env.lookup("d"));
}
