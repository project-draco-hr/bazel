{
  if (!(o instanceof Key)) {
    return false;
  }
  Key otherConfig=(Key)o;
  return Objects.equals(fragments,otherConfig.fragments) && Objects.equals(buildOptions,otherConfig.buildOptions) && otherConfig.actionsEnabled() == enableActions;
}
