{
  Preconditions.checkState(this.preprocessorFactorySupplier == null,"At most one module defines a preprocessor factory supplier. But found two: %s and %s",this.preprocessorFactorySupplier,preprocessorFactorySupplier);
  this.preprocessorFactorySupplier=Preconditions.checkNotNull(preprocessorFactorySupplier);
  return this;
}
