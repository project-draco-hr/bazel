{
  assertEquals("RPAREN NEWLINE EOF",names(tokens(")")));
  assertEquals("RPAREN NEWLINE EOF",names(tokens(" )")));
  assertEquals("RPAREN NEWLINE EOF",names(tokens(" ) ")));
  assertEquals("RPAREN NEWLINE EOF",names(tokens(") ")));
}
