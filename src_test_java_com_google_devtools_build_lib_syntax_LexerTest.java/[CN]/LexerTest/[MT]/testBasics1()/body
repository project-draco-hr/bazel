{
  assertEquals("IDENTIFIER RPAREN NEWLINE EOF",names(tokens("wiz) ")));
  assertEquals("IDENTIFIER RPAREN NEWLINE EOF",names(tokens("wiz )")));
  assertEquals("IDENTIFIER RPAREN NEWLINE EOF",names(tokens(" wiz)")));
  assertEquals("IDENTIFIER RPAREN NEWLINE EOF",names(tokens(" wiz ) ")));
  assertEquals("IDENTIFIER RPAREN NEWLINE EOF",names(tokens("wiz\t)")));
}
