{
  if (!(other instanceof ArtifactFile)) {
    return false;
  }
  ArtifactFile that=(ArtifactFile)other;
  return this.getParent().equals(that.getParent()) && this.getParentRelativePath().equals(that.getParentRelativePath());
}
