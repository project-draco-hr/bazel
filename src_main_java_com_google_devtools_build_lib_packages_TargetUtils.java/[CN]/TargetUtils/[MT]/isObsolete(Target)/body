{
  if (!(target instanceof Rule)) {
    return false;
  }
  Rule rule=(Rule)target;
  return (rule.isAttrDefined("obsolete",Type.BOOLEAN)) && NonconfigurableAttributeMapper.of(rule).get("obsolete",Type.BOOLEAN);
}
