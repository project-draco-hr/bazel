{
  if (this == other) {
    return true;
  }
  if (!(other instanceof Aspect)) {
    return false;
  }
  Aspect that=(Aspect)other;
  return Objects.equals(this.aspectClass,that.aspectClass) && Objects.equals(this.parameters,that.parameters);
}
