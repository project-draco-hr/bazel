{
  super(BuildInfoHelper.BUILD_INFO_ACTION_OWNER,Artifact.NO_ARTIFACTS,ImmutableList.of(stableStatus,volatileStatus));
  this.options=Preconditions.checkNotNull(options);
  this.stableStatus=stableStatus;
  this.volatileStatus=volatileStatus;
  this.username=USER_NAME.value();
  this.hostname=NetUtil.findShortHostName();
  this.timestamp=System.currentTimeMillis();
  this.getWorkspaceStatusCommand=options.workspaceStatusCommand.equals(PathFragment.EMPTY_FRAGMENT) ? null : new CommandBuilder().addArgs(options.workspaceStatusCommand.toString()).setEnv(runtime.getClientEnv()).setWorkingDir(runtime.getWorkspace()).useShell(true).build();
}
