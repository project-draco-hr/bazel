{
  return builder.add(attr("hdrs",LABEL_LIST).orderIndependent().direct_compile_time_input().allowedFileTypes(CPP_HEADER)).add(attr("linkstamp",LABEL).allowedFileTypes(CPP_SOURCE,C_SOURCE)).build();
}
