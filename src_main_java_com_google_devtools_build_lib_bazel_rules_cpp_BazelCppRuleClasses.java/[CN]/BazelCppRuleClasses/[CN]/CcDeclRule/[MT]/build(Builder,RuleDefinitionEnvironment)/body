{
  return builder.add(attr("abi",STRING).value("$(ABI)")).add(attr("abi_deps",LABEL_LIST_DICT)).add(attr("defines",STRING_LIST)).add(attr("includes",STRING_LIST)).add(attr(":lipo_context_collector",LABEL).cfg(CppTransition.LIPO_COLLECTOR).value(LIPO_CONTEXT_COLLECTOR).skipConstraintsCheck()).build();
}
