{
  try {
    Package pkg=factory.createPackageForTesting(packageIdentifier,buildFile,getPackageLocator(),reporter);
    return pkg;
  }
 catch (  InterruptedException e) {
    throw new IllegalStateException(e);
  }
}
