{
  try {
    Package pkg=factory.createPackageForTesting(PackageIdentifier.createInDefaultRepo(packageName),buildFile,getPackageLocator(),reporter);
    return pkg;
  }
 catch (  InterruptedException e) {
    throw new IllegalStateException(e);
  }
}
