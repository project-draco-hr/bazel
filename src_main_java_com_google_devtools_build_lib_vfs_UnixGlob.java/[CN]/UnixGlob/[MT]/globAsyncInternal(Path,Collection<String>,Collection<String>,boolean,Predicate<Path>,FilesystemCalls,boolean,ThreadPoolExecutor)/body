{
  Preconditions.checkNotNull(threadPool,"%s %s",base,patterns);
  try {
    return new GlobVisitor(threadPool,checkForInterruption).globAsync(base,patterns,excludePatterns,excludeDirectories,dirPred,syscalls);
  }
 catch (  IOException e) {
    throw new IllegalStateException(e);
  }
}
