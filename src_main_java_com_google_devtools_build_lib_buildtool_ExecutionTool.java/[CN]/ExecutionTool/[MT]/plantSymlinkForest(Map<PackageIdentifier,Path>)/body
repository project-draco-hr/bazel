{
  try {
    SymlinkForest forest=new SymlinkForest(packageRoots,getExecRoot(),runtime.getProductName());
    forest.plantLinkForest();
  }
 catch (  IOException e) {
    throw new ExecutorInitException("Source forest creation failed",e);
  }
}
