{
  NoSuchTargetException errorLoadingTarget=targetAndErrorIfAny.getErrorLoadingTarget();
  return errorLoadingTarget == null ? TransitiveTraversalValue.SUCCESSFUL_TRANSITIVE_TRAVERSAL_VALUE : TransitiveTraversalValue.unsuccessfulTransitiveTraversal(errorLoadingTarget);
}
