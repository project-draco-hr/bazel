{
  if (isAttrDefined("licenses",Type.LICENSE) && isAttributeValueExplicitlySpecified("licenses")) {
    return NonconfigurableAttributeMapper.of(this).get("licenses",Type.LICENSE);
  }
 else {
    return getPackage().getDefaultLicense();
  }
}
