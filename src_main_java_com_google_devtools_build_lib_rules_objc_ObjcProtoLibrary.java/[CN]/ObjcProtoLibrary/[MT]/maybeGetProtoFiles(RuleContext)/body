{
  PrerequisiteArtifacts prerequisiteArtifacts=ruleContext.getPrerequisiteArtifacts("deps",Mode.TARGET);
  if (!prerequisiteArtifacts.list().isEmpty()) {
    ruleContext.attributeWarning("deps","Using files and filegroups in objc_proto_library is deprecated");
  }
  return prerequisiteArtifacts.filter(FileType.of(".proto")).list();
}
