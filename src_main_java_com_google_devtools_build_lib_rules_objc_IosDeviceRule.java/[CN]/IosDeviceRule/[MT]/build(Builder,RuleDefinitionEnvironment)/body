{
  return builder.add(attr("ios_version",STRING).mandatory()).add(attr("type",STRING).mandatory()).add(attr("locale",STRING).undocumented("this is not yet supported by any test runner").value("en")).build();
}
