{
  return builder.requiresConfigurationFragments(AppleConfiguration.class).add(attr("ios_version",STRING)).add(attr("type",STRING).mandatory()).add(attr("locale",STRING).undocumented("this is not yet supported by any test runner").value("en")).build();
}
