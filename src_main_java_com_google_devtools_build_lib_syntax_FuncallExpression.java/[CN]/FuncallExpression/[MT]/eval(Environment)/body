{
  try {
    return (obj != null) ? invokeObjectMethod(env) : invokeGlobalFunction(env);
  }
 catch (  EvalException ex) {
    throw (ex instanceof EvalExceptionWithStackTrace) ? ex : new EvalExceptionWithStackTrace(ex,ex.getLocation());
  }
}
