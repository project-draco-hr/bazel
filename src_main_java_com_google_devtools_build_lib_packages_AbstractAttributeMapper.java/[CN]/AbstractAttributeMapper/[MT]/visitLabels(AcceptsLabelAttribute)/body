{
  for (  Attribute attribute : ruleClass.getAttributes()) {
    Type<?> type=attribute.getType();
    if (type != Type.OUTPUT && type != Type.OUTPUT_LIST && type != Type.NODEP_LABEL && type != Type.NODEP_LABEL_LIST) {
      visitLabels(attribute,observer);
    }
  }
}
