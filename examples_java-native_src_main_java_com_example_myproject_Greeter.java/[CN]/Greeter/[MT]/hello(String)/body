{
  String greeting="Hello";
  InputStream stream=Greeter.class.getResourceAsStream("/examples/java-native/src/main/resources/greeting.txt");
  if (stream != null) {
    greeting=convertStreamToString(stream);
  }
  out.println(greeting + " " + obj);
}
