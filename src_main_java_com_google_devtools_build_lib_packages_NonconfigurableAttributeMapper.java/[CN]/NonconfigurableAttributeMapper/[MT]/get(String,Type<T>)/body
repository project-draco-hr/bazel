{
  Preconditions.checkState(!getAttributeDefinition(attributeName).isConfigurable(),"Attribute '" + attributeName + "' is potentially configurable - not allowed here");
  return super.get(attributeName,type);
}
