{
  newTest().testIfExactError("in operator only works on strings if the left operand is also a string","1 in '123'").testIfExactError("in operator only works on lists, tuples, sets, dicts and strings","'a' in 1");
}
