{
  checkEvalError("1 in '123'","in operator only works on strings if the left operand is also a string");
  checkEvalError("'a' in 1","in operator only works on lists, tuples, sets, dicts and strings");
}
