{
  assertEquals(Arrays.asList(1,2,3,4),eval("[1, 2] + [3, 4]"));
  assertEquals(ImmutableList.of(1,2,3,4),eval("(1, 2) + (3, 4)"));
  checkEvalError("can only concatenate Tuple (not \"List\") to Tuple","[1, 2] + (3, 4)");
  checkEvalError("can only concatenate List (not \"Tuple\") to List","(1, 2) + [3, 4]");
}
