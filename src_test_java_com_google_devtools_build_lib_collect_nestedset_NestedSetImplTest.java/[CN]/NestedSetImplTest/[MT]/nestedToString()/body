{
  NestedSet<String> b=nestedSetBuilder("b").build();
  NestedSet<String> c=nestedSetBuilder("c").build();
  assertEquals("{a, {b}}",nestedSetBuilder("a").addTransitive(b).build().toString());
  assertEquals("{a, {b}, {c}}",nestedSetBuilder("a").addTransitive(b).addTransitive(c).build().toString());
  assertEquals("{b}",nestedSetBuilder().addTransitive(b).build().toString());
}
