{
  Target target=getPackageManager().getTarget(reporter,label);
  return (runtime.getOutputService() != null) ? runtime.getOutputService().stageTool(target) : target.getPackage().getPackageDirectory().getRelative(target.getName());
}
