{
  assertEquals(unixFs.getPath("/"),unixFs.getPath("/.."));
  assertEquals(unixFs.getPath("/"),unixFs.getPath("/../../../../../.."));
  assertEquals(unixFs.getPath("/foo"),unixFs.getPath("/../../../foo"));
}
