{
  return new CommandLine(){
    @Override public Iterable<String> arguments(){
      return new ImmutableList.Builder<String>().addAll(Interspersing.beforeEach("--source_file",Artifact.toExecPaths(inputPlists))).add("--out_file",mergedInfoplist.getExecPathString()).build();
    }
  }
;
}
