{
  createOutputDirectory();
  return SkylarkExecutionResult.builder(osObject.getEnvironmentVariables()).setDirectory(outputDirectory.getPathFile()).addArguments(arguments).setTimeout(timeout.longValue() * 1000).execute();
}
