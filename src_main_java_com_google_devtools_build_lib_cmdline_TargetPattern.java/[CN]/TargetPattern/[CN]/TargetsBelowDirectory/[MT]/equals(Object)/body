{
  if (this == o) {
    return true;
  }
  if (!(o instanceof TargetsBelowDirectory)) {
    return false;
  }
  TargetsBelowDirectory that=(TargetsBelowDirectory)o;
  return rulesOnly == that.rulesOnly && originalPattern.equals(that.originalPattern) && directory.equals(that.directory);
}
