{
  NodeEntry entry=Preconditions.checkNotNull(graph.getBatchWithFieldHints(ImmutableList.of(key),NodeEntryField.VALUE_ONLY).get(key),key);
  Preconditions.checkState(entry.isDone(),"%s %s",key,entry);
  return entry;
}
