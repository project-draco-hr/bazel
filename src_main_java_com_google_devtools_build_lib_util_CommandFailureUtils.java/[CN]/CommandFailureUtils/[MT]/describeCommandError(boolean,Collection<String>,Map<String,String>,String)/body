{
  CommandDescriptionForm form=verbose ? CommandDescriptionForm.COMPLETE : CommandDescriptionForm.ABBREVIATED;
  return "error executing command " + (verbose ? "\n  " : "") + describeCommand(form,commandLineElements,env,cwd);
}
