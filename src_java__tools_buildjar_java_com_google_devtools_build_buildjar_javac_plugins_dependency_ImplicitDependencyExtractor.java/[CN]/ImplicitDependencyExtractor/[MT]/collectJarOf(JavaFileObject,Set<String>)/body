{
  String name=getJarName(fileManager,reference);
  if (name == null) {
    return;
  }
  if (platformJars.contains(name)) {
    return;
  }
  depsSet.add(name);
  if (!depsMap.containsKey(name)) {
    depsMap.put(name,Deps.Dependency.newBuilder().setKind(Deps.Dependency.Kind.IMPLICIT).setPath(name).build());
  }
}
