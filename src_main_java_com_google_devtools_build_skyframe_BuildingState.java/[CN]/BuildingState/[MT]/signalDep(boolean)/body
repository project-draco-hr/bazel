{
  signaledDeps++;
  if (isDirty() && !isChanged()) {
    if (childChanged) {
      dirtyState=DirtyState.NEEDS_REBUILDING;
    }
 else     if (dirtyState == DirtyState.CHECK_DEPENDENCIES && isReady() && !dirtyDirectDepIterator.hasNext()) {
      dirtyState=DirtyState.VERIFIED_CLEAN;
    }
  }
  return isReady();
}
