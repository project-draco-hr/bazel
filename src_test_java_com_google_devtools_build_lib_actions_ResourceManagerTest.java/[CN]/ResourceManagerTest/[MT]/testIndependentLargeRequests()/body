{
  assertFalse(rm.inUse());
  acquire(10000,0,0);
  acquire(0,100,0);
  acquire(0,0,1);
  assertTrue(rm.inUse());
  release(9500,0,0);
  acquire(400,0,0);
  release(0,99.5,0.6);
  acquire(100,0.5,0.4);
  release(1000,1,1);
  assertFalse(rm.inUse());
}
