{
  assertFalse(rm.inUse());
  acquire(900,0.5,0.1);
  acquire(100,0.6,0.9);
  release(100,0.6,0.9);
  acquire(100,0.1,0.1);
  acquire(0,0.5,0.8);
  release(1020,1.1,1.05);
  assertFalse(rm.inUse());
}
