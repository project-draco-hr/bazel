{
  syntaxEvents.setFailFast(false);
  parseFileForSkylark("def func(a, b1=2, b2=3, *, c1, c2, d=4): return a + b1 + b2 + c1 + c2 + d\n");
  syntaxEvents.assertContainsEvent("no star, star-star or named-only parameters (for now)");
}
