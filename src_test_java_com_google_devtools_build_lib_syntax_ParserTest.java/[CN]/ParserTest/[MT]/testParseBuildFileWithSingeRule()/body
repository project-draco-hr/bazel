{
  List<Statement> stmts=parseFile("genrule(name = 'foo'," + '\n' + "   srcs = ['input.csv'],"+ '\n'+ "   outs = [ 'result.txt',"+ '\n'+ "           'result.log'],"+ '\n'+ "   cmd = 'touch result.txt result.log')"+ '\n');
  assertThat(stmts).hasSize(1);
}
