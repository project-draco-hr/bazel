{
  syntaxEvents.setFailFast(false);
  parseFileForSkylark("def func(a):\n" + "  def bar(): return 0\n" + "  return bar()\n");
  syntaxEvents.assertContainsEvent("nested functions are not allowed. Move the function to top-level");
}
