{
  if (common.getCompilationArtifacts().isPresent()) {
    ObjcRuleClasses.actionsBuilder(ruleContext).registerCompileAndArchiveActions(common.getCompilationArtifacts().get(),common.getObjcProvider(),optionsProvider);
  }
  return this;
}
