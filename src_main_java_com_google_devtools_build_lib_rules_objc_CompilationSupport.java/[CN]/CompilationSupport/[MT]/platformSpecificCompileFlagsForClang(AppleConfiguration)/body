{
switch (Platform.forArch(configuration.getIosCpu())) {
case IOS_DEVICE:
    return ImmutableList.of();
case IOS_SIMULATOR:
  return ImmutableList.of("-fexceptions","-fasm-blocks","-fobjc-abi-version=2","-fobjc-legacy-dispatch");
default :
throw new AssertionError();
}
}
