{
  ArrayList<Artifact> headers=new ArrayList<>();
  for (  J2ObjcSource j2ObjcSource : provider.getSrcs()) {
    headers.addAll(ImmutableList.copyOf(j2ObjcSource.getObjcHdrs()));
  }
  return registerGenerateModuleMapAction(ObjcRuleClasses.intermediateArtifacts(ruleContext).moduleMap(),headers,ImmutableList.<Artifact>of(),ImmutableList.<CppModuleMap>of());
}
