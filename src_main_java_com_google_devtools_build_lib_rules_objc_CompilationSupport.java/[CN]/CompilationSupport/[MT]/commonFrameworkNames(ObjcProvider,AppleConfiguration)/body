{
  Platform platform=appleConfiguration.getSingleArchPlatform();
  return new ImmutableList.Builder<String>().add(AppleToolchain.sdkFrameworkDir(platform,appleConfiguration)).add(AppleToolchain.platformDeveloperFrameworkDir(appleConfiguration)).addAll(PathFragment.safePathStrings(uniqueParentDirectories(provider.get(FRAMEWORK_DIR)))).addAll(Interspersing.beforeEach("-F",PathFragment.safePathStrings(uniqueParentDirectories(provider.get(FRAMEWORK_SEARCH_PATH_ONLY))))).build();
}
