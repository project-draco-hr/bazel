{
  if (usesAnnotationProcessing()) {
    return getAnalysisEnvironment().getDerivedArtifact(FileSystemUtils.appendWithoutExtension(outputJar.getRootRelativePath(),"-gensrc"),outputJar.getRoot());
  }
 else {
    return null;
  }
}
