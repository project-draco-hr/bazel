{
  Artifact jar=getJavaConfiguration().getUseIjars() ? createIjarAction(runtimeJar,false) : runtimeJar;
  Artifact deps=runtimeDeps;
  builder.addCompileTimeJar(jar);
  builder.setCompileTimeDependencies(deps);
}
