{
  if (verboseFailures) {
    return new ActionExecutionException(messagePrefix + " failed" + getMessage(),this,action,isCatastrophic());
  }
 else {
    return new ActionExecutionException(messagePrefix + " failed" + getMessage(),action,isCatastrophic());
  }
}
