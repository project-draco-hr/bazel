{
  if (subdirectoryTransitivelyContainsPackages.isEmpty()) {
    if (isDirectoryPackage) {
      return EMPTY_DIRECTORY_PACKAGE;
    }
 else {
      return EMPTY;
    }
  }
 else {
    return new PrepareDepsOfTargetsUnderDirectoryValue(isDirectoryPackage,subdirectoryTransitivelyContainsPackages);
  }
}
