{
  artifactFactory.clear();
  PathFragment randomPath=new PathFragment("maracuja/lemon/kiwi");
  Artifact artifact=artifactFactory.deserializeArtifact(randomPath,new MockPackageRootResolver());
  assertNull(artifact);
}
