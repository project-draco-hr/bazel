{
  BuildFileAST buildFileAST=parseBuildFile("foo('a')\n","subinclude()\n","subinclude('hello')\n","1 + subinclude('ignored')","var = 'also ignored'","subinclude(var)\n","subinclude('world')");
  assertThat(buildFileAST.getSubincludes()).containsExactly("hello","world");
}
