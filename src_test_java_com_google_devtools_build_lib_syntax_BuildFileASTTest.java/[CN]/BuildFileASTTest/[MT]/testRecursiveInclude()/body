{
  setFailFast(false);
  Path buildFile=scratch.file("/foo/bar/BUILD","include(\"//foo/bar:BUILD\")\n");
  BuildFileAST.parseBuildFile(buildFile,getEventHandler(),locator,false);
  assertContainsEvent("Recursive inclusion");
}
