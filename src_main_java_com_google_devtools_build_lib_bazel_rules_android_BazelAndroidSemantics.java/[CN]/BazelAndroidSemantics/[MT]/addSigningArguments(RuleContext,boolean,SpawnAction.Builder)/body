{
  if (sign) {
    Artifact debugKeyStore=ruleContext.getPrerequisiteArtifact("$debug_keystore",Mode.HOST);
    actionBuilder.addInput(debugKeyStore).setEnvironment(ImmutableMap.of("KEYSTORE",debugKeyStore.getExecPath().getPathString()));
  }
 else {
    actionBuilder.addArgument("-u");
  }
}
