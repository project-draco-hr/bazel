{
  Preconditions.checkState(lastBuildKeepGoing);
  return Multimaps.filterKeys(rootCauses,new Predicate<Label>(){
    @Override public boolean apply(    Label label){
      return targetsToLoad.contains(label);
    }
  }
);
}
