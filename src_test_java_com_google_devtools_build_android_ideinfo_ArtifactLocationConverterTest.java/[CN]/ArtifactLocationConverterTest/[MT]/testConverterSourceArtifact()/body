{
  ArtifactLocation parsed=converter.convert(Joiner.on(",").join("","test.java","/usr/local/code"));
  assertThat(parsed).isEqualTo(ArtifactLocation.newBuilder().setRootPath("/usr/local/code").setRelativePath("test.java").setIsSource(true).build());
}
