{
  ArtifactLocation parsed=converter.convert(Joiner.on(",").join("","test.java","/usr/local/code"));
  assertThat(parsed).isEqualTo(ArtifactLocation.newBuilder().setRootPath(Paths.get("/usr/local/code").toString()).setRelativePath(Paths.get("test.java").toString()).setIsSource(true).build());
}
