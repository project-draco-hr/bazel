{
  ArtifactLocation parsed=converter.convert("bin/out,java/com/test.java");
  assertThat(parsed).isEqualTo(ArtifactLocation.newBuilder().setRootExecutionPathFragment("bin/out").setRelativePath("java/com/test.java").setIsSource(false).build());
}
