{
  this.groupId=groupId;
  this.artifactId=artifactId;
  this.version=version;
  this.outputDirectory=outputDirectory;
  this.repositories=new ArrayList<>(Arrays.asList(new RemoteRepository.Builder("central","default",MAVEN_CENTRAL_URL).build()));
}
