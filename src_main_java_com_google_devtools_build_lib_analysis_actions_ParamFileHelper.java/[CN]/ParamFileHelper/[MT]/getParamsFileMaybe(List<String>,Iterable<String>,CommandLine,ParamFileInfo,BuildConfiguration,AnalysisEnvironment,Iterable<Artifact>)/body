{
  if (paramFileInfo == null || getParamFileSize(executableArgs,arguments,commandLine) < configuration.getMinParamFileSize()) {
    return null;
  }
  return getParamsFile(analysisEnvironment,configuration,Iterables.getFirst(outputs,null));
}
