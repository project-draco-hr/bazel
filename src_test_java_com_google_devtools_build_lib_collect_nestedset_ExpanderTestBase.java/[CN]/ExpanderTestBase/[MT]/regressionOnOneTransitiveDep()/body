{
  NestedSet<String> subsub=NestedSetBuilder.<String>stableOrder().add("c").add("a").add("e").build();
  NestedSet<String> sub=NestedSetBuilder.<String>stableOrder().add("b").add("d").addTransitive(subsub).build();
  NestedSet<String> top=prepareBuilder().addTransitive(sub).build();
  assertSetContents(nestedResult(),top);
}
