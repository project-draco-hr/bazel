{
  ImmutableList<Artifact> declaredIncludeSrcs=ImmutableList.copyOf(context.getDeclaredIncludeSrcs());
  for (  Artifact linkstamp : linkstamps) {
    linkstampsBuilder.add(new Linkstamp(linkstamp,declaredIncludeSrcs));
  }
  return this;
}
