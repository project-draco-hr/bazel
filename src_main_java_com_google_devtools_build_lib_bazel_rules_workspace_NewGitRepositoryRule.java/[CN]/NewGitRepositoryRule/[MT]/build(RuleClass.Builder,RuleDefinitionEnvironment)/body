{
  return builder.add(attr("remote",STRING).mandatory()).add(attr("commit",STRING)).add(attr("tag",STRING)).add(attr("build_file",STRING).mandatory()).add(attr("init_submodules",BOOLEAN).value(false)).setWorkspaceOnly().build();
}
