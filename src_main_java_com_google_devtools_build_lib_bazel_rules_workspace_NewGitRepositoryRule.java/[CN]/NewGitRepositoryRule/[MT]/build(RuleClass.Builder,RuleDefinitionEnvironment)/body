{
  return builder.add(attr("remote",STRING).mandatory()).add(attr("commit",STRING)).add(attr("tag",STRING)).add(attr("build_file",STRING)).add(attr("build_file_content",STRING)).add(attr("init_submodules",BOOLEAN).value(false)).setWorkspaceOnly().build();
}
