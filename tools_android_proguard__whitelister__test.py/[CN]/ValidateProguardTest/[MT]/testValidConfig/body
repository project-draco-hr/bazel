def testValidConfig(self):
    path = os.path.join(os.path.dirname(__file__), 'proguard_whitelister_input.cfg')
    with open(path) as config:
        self.assertEqual([], proguard_whitelister.Validate(config.read()))
