def testInvalidOptimizationConfig(self):
    self.assertEqual(['-optimizations'], proguard_whitelister.Validate("#We don't want libraries disabling global optimizations.\n        -optimizations !class/merging/*,!code/allocation/variable\n        "))
