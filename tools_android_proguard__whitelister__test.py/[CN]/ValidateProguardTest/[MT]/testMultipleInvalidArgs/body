def testMultipleInvalidArgs(self):
    self.assertEqual(['-optimizations', '-dontnote'], proguard_whitelister.Validate("#We don't want libraries disabling global optimizations.\n            -optimizations !class/merging/*,!code/allocation/variable\n            -dontnote\n            "))
