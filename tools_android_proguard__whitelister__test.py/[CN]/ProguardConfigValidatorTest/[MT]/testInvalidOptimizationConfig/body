def testInvalidOptimizationConfig(self):
    self._TestInvalidConfig(['-optimizations'], '# We don"t want libraries disabling global optimizations.\n-optimizations !class/merging/*,!code/allocation/variable')
