{
  String prefix="Frame";
  StringBuilder sb=new StringBuilder();
  for (Frame f=this; f != null; f=f.parent) {
    Printer.formatTo(sb,"%s%s%r",ImmutableList.<Object>of(prefix,f.mutability(),f.bindings));
    prefix="=>";
  }
  return sb.toString();
}
