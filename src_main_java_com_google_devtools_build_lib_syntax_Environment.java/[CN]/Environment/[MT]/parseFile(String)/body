{
  ParserInputSource input=ParserInputSource.create(Joiner.on("\n").join(inputLines),null);
  Parser.ParseResult result=isSkylark ? Parser.parseFileForSkylark(input,eventHandler,new ValidationEnvironment(this)) : Parser.parseFile(input,eventHandler,false);
  return result.statements;
}
