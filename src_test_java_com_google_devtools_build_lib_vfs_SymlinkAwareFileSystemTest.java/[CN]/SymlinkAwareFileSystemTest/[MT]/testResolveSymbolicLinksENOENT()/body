{
  if (supportsSymlinks) {
    try {
      xDanglingLink.resolveSymbolicLinks();
      fail();
    }
 catch (    IOException e) {
      assertEquals(xNothing + " (No such file or directory)",e.getMessage());
    }
  }
}
