{
  return new IllegalStateException(String.format("%s%s (%s)\n" + "while calling %s with args %s\nJava parameter types: %s\nSkylark type checks: %s",(loc == null) ? "" : loc + ": ",e.getClass().getName(),e.getMessage(),this,Arrays.asList(args),Arrays.asList(invokeMethod.getParameterTypes()),signature.getTypes()),e);
}
