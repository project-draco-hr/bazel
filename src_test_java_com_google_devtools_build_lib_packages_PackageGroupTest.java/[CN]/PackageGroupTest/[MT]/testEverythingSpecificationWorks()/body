{
  scratch.file("fruits/BUILD","package_group(name = 'mango', packages = ['//...'])");
  PackageGroup packageGroup=getPackageGroup("fruits","mango");
  MoreAsserts.assertSameContents(ImmutableList.of(PackageSpecification.EVERYTHING),packageGroup.getPackageSpecifications());
}
