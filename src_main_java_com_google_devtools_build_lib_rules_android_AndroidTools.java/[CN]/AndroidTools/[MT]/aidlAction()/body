{
  return androidSdk != null ? new SpawnAction.Builder().setExecutable(androidSdk.getAidl()) : new SpawnAction.Builder().addInputs(toolRunner.getRunfilesSupport().getRunfilesArtifactsWithoutMiddlemen()).setExecutable(toolRunner.getExecutable()).addInputArgument(aidlTool);
}
