{
  return androidSdk != null ? new SpawnAction.Builder().addInput(androidSdk.getAidl().getExecutable()).setExecutable(androidSdk.getAidl()) : new SpawnAction.Builder().addInput(aidlTool).setExecutable(aidlTool);
}
