{
  for (  String attribute : ImmutableSet.of("deps","non_propagated_deps","bundles")) {
    xcodeProviderBuilder.addDependencies(ruleContext.getPrerequisites(attribute,Mode.TARGET,XcodeProvider.class));
  }
  return this;
}
