{
  Preconditions.checkNotNull(attribute);
  for (  Class<? extends AspectFactory<?,?,?>> aspectFactory : aspectFactories) {
    this.attributeAspects.put(attribute,new NativeAspectClass(Preconditions.checkNotNull(aspectFactory)));
  }
  return this;
}
