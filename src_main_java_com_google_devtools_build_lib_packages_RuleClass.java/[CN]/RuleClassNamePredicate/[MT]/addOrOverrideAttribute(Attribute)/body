{
  String name=attribute.getName();
  boolean override=attributes.containsKey(name);
  Preconditions.checkArgument(!override,"There is already a built-in attribute '%s' which cannot be overridden",name);
  if (override) {
    overrideAttribute(attribute);
  }
 else {
    addAttribute(attribute);
  }
}
