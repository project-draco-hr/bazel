{
  int uriStart=url.indexOf(getUri());
  if (uriStart == -1) {
    throw new InvalidRuleException("Cannot find expected URI (" + getUri() + ") in URL ("+ url+ ")");
  }
  this.repository=url.substring(0,uriStart);
}
