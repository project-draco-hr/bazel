{
  PathFragment relativePath=Util.getWorkspaceRelativePath(ruleContext.getTarget());
  PathFragment linkedFileName=(linkType == LinkTargetType.EXECUTABLE) ? relativePath : relativePath.replaceName("lib" + relativePath.getBaseName() + linkType.getExtension());
  return linkedFileName;
}
