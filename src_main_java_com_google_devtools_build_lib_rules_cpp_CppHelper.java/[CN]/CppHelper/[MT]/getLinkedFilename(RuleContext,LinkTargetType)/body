{
  PathFragment result=ruleContext.getPackageDirectory().getRelative(ruleContext.getLabel().getName());
  if (linkType != LinkTargetType.EXECUTABLE) {
    result=result.replaceName("lib" + result.getBaseName() + linkType.getExtension());
  }
  return result;
}
