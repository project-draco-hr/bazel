{
  Preconditions.checkArgument(isLegalFragment(fragment,config),"%s does not have access to '%s' in %s configuration",rule.getRuleClass(),fragment.getSimpleName(),FragmentCollection.getConfigurationName(config));
  return getConfiguration(config).getFragment(fragment);
}
