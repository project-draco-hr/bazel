{
  return new ActionOwner(rule.getLabel(),rule.getLocation(),configuration.getMnemonic(),rule.getTargetKind(),configuration.checksum(),configuration.isHostConfiguration() ? HOST_CONFIGURATION_PROGRESS_TAG : null);
}
