{
  for (  String provider : attribute.getMandatoryProviders()) {
    if (prerequisite.get(provider) == null) {
      attributeError(attribute.getName(),"'" + prerequisite.getLabel() + "' does not have mandatory provider '"+ provider+ "'");
    }
  }
}
