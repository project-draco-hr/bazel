{
  return builder.setUndocumented().add(attr("source_version",STRING).mandatory()).add(attr("target_version",STRING).mandatory()).add(attr("encoding",STRING).mandatory()).add(attr("xlint",STRING_LIST).value(ImmutableList.<String>of())).add(attr("misc",STRING_LIST).value(ImmutableList.<String>of())).build();
}
