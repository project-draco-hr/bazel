{
  if (this == other) {
    return true;
  }
  if (!(other instanceof MergeConflict)) {
    return false;
  }
  MergeConflict that=(MergeConflict)other;
  return Objects.equals(dataKey,that.dataKey) && Objects.equals(first,that.first) && Objects.equals(second,that.second);
}
