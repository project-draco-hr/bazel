{
  if (!(other instanceof Label)) {
    return false;
  }
  Label otherLabel=(Label)other;
  return hashCode == otherLabel.hashCode && name.equals(otherLabel.name) && packageIdentifier.equals(otherLabel.packageIdentifier);
}
