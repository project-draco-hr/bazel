{
  if (parent == null) {
    return "";
  }
  if (parent.isEmpty()) {
    return "parent=''";
  }
  if (parent.startsWith("style/")) {
    return "parent='@" + parent + "'";
  }
  return "parent='@style/" + parent + "'";
}
