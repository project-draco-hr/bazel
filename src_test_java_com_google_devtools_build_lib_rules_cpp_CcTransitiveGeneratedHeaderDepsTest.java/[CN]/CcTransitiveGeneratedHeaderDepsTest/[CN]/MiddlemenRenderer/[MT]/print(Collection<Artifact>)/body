{
  for (  Artifact middleman : middlemenOrHeaders) {
    printIndent();
    if (isMiddleman(middleman)) {
      printMiddleman(middleman);
      indent+=2;
      List<Artifact> inputs=Ordering.from(Artifact.EXEC_PATH_COMPARATOR).sortedCopy(getGeneratingAction(middleman).getInputs());
      print(inputs);
      indent-=2;
    }
 else {
      printPrerequisite(middleman);
    }
  }
}
