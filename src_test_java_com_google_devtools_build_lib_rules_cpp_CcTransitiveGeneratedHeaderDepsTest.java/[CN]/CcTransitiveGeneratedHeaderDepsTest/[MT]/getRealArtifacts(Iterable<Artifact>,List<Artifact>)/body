{
  for (  Artifact file : middlemenOrHeaders) {
    if (isMiddleman(file)) {
      getRealArtifacts(getGeneratingAction(file).getInputs(),artifacts);
    }
 else {
      artifacts.add(file);
    }
  }
}
