{
  return Iterables.filter(quoteIncludeDirs,new Predicate<PathFragment>(){
    @Override public boolean apply(    PathFragment pathFragment){
      return !pathFragment.endsWith(new PathFragment("external/bazel_tools"));
    }
  }
);
}
