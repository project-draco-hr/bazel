def testMergeWithNoApplication(self):
    merger = merge_manifests.MergeManifests((INVALID_MERGER_MANIFEST, 'INVALID_MERGER_MANIFEST'), [(INVALID_MERGEE_MANIFEST, 'INVALID_MERGEE_MANIFEST')], ['all'])
    merger.Merge()
