{
  addInputs(tool.getFilesToRun());
  if (tool.getRunfilesManifest() != null) {
    addInputManifest(tool.getRunfilesManifest(),BaseSpawn.runfilesForFragment(tool.getExecutable().getExecPath()));
  }
  return this;
}
