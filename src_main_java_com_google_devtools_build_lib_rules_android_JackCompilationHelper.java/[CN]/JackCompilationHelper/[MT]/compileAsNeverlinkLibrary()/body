{
  JackLibraryProvider nonNeverlink=compileAsLibrary();
  return new JackLibraryProvider(NestedSetBuilder.<Artifact>emptySet(Order.NAIVE_LINK_ORDER),nonNeverlink.getTransitiveJackClasspathLibraries());
}
