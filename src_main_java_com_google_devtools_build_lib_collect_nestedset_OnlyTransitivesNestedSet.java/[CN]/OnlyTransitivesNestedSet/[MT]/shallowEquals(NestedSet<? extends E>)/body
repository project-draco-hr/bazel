{
  if (this == other) {
    return true;
  }
  return other != null && getOrder().equals(other.getOrder()) && other instanceof OnlyTransitivesNestedSet && Arrays.equals(transitives,((OnlyTransitivesNestedSet)other).transitives);
}
