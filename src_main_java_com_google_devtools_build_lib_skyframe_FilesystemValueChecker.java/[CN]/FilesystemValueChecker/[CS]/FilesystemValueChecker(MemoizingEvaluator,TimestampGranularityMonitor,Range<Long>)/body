{
  this.tsgm=tsgm;
  this.lastExecutionTimeRange=lastExecutionTimeRange;
  this.valuesSupplier=Suppliers.memoize(new Supplier<Map<SkyKey,SkyValue>>(){
    @Override public Map<SkyKey,SkyValue> get(){
      return evaluator.getValues();
    }
  }
);
}
