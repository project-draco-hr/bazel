{
  checkArgument(dexArchive.getFilename().endsWith(".dex.zip"),"Doesn't look like a dex archive: %s",dexArchive);
  Artifact old=dexArchives.put(checkNotNull(dexedJar,"dexedJar"),dexArchive);
  checkArgument(old == null || old.equals(dexedJar),"We already had mapping %s-%s, so we don't also need %s",dexedJar,old,dexArchive);
  return this;
}
