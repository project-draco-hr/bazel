{
  InputStream centralDirectory=new BufferedInputStream(Channels.newInputStream(in.getChannel().position(fileOffset)));
  for (long i=0; i < count; i++) {
    ZipFileEntry entry=CentralDirectoryFileHeader.read(centralDirectory,zipData.getCharset());
    zipData.addEntry(entry);
  }
}
