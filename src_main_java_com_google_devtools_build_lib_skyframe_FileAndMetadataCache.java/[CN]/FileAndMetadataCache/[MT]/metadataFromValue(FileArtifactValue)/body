{
  if (value == FileArtifactValue.MISSING_FILE_MARKER) {
    throw new FileNotFoundException();
  }
  return value.getSize() > 0 ? new Metadata(value.getDigest()) : new Metadata(value.getModifiedTime());
}
