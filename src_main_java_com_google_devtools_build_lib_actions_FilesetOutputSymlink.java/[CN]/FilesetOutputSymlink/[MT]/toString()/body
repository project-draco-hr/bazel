{
  if (metadata.equals(STRIPPED_METADATA)) {
    return String.format("FilesetOutputSymlink(%s -> %s)",name.getPathString(),target.getPathString());
  }
 else {
    return String.format("FilesetOutputSymlink(%s -> %s | metadata=%s)",name.getPathString(),target.getPathString(),metadata);
  }
}
