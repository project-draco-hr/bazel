{
  final Pair<Map<PathFragment,Artifact>,Map<PathFragment,Artifact>> runfilesInputs=runfiles.getRunfilesInputs(root,executor.getContext(Context.class).getRunfilesPrefix().toString(),eventHandler,getOwner().getLocation());
  return new DeterministicWriter(){
    @Override public void writeOutputFile(    OutputStream out) throws IOException {
      writeFile(out,runfilesInputs);
    }
  }
;
}
