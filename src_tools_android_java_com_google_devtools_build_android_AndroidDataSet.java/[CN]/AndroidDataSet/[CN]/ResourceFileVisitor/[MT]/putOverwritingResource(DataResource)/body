{
  if (overwritingResources.containsKey(dataResource.dataKey())) {
    conflicts.add(MergeConflict.between(dataResource.dataKey(),dataResource,overwritingResources.get(dataResource.dataKey())));
  }
 else {
    overwritingResources.put(dataResource.dataKey(),dataResource);
  }
}
