{
  Preconditions.checkState(workspace != null);
  Preconditions.checkState(productName != null);
  return new SymlinkForest(legacyExternalRunfiles,packageRoots,workspace,productName,prefixes,workspaceName);
}
