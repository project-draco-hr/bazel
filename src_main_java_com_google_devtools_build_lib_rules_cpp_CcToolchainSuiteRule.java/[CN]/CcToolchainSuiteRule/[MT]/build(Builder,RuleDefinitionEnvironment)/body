{
  return builder.setUndocumented().add(attr("toolchains",BuildType.LABEL_DICT_UNARY).mandatory().nonconfigurable("Used during configuration creation")).add(attr("proto",Type.STRING).nonconfigurable("Used during configuration creation")).build();
}
