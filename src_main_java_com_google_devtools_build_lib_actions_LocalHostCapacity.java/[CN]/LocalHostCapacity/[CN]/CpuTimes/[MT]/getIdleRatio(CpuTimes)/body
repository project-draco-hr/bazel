{
  if (prevTimes.totalJiffies == 0 || totalJiffies == prevTimes.totalJiffies) {
    return 0;
  }
  return ((double)(idleJiffies - prevTimes.idleJiffies) / (double)(totalJiffies - prevTimes.totalJiffies));
}
