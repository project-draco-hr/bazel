{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof ResourceContainer)) {
    return false;
  }
  ResourceContainer other=(ResourceContainer)obj;
  return Objects.equals(label,other.label) && Objects.equals(rTxt,other.rTxt) && Objects.equals(symbolsTxt,other.symbolsTxt);
}
