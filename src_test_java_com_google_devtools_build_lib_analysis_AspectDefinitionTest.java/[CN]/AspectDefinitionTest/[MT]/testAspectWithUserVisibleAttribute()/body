{
  try {
    new AspectDefinition.Builder("user_visible_attribute").add(attr("invalid",Type.LABEL).value(Label.parseAbsoluteUnchecked("//run:time"))).attributeAspect("deps",TestAspectFactory.class).build();
    fail();
  }
 catch (  IllegalStateException e) {
  }
}
