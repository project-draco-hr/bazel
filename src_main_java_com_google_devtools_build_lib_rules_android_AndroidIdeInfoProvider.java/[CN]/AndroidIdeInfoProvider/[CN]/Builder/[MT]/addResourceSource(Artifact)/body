{
  PathFragment resourceDir=LocalResourceContainer.Builder.findResourceDir(resource);
  resourceDirs.add(new SourceDirectory(resource.getRoot().getPath().asFragment(),trimTo(resource.getRootRelativePath(),resourceDir),resource.isSourceArtifact()));
  return this;
}
