{
  resourceDirs.add(new SourceDirectory(resource.getRoot().getPath().asFragment(),trimTo(resource.getRootRelativePath(),LocalResourceContainer.Builder.findResourceDir(resource)),resource.isSourceArtifact()));
  return this;
}
