{
  if (!skyframeExecutor.hasIncrementalState()) {
    clearSkyframeRelevantCaches();
  }
  skyframeExecutor.sync(eventHandler,packageCacheOptions,getOutputBase(),getWorkingDirectory(),defaultsPackageContents,commandId);
}
