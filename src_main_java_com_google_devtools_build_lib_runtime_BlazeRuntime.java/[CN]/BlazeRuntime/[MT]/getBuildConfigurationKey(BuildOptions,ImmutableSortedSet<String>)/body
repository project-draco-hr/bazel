{
  Map<String,String> testEnv=computeTestEnv(buildOptions.get(BuildConfiguration.Options.class).testEnvironment,clientEnv);
  return new BuildConfigurationKey(buildOptions,directories,testEnv,multiCpu);
}
