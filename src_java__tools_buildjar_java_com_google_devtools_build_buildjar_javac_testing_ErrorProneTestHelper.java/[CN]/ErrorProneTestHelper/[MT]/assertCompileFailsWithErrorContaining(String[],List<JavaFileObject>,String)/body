{
  List<Diagnostic<? extends JavaFileObject>> diagnostics=assertCompileFailsWithOneError(args,sources);
  assertTrue("Error message should contain " + errorMsg + ", was: "+ diagnostics.get(0).getMessage(null),diagnostics.get(0).getMessage(null).contains(errorMsg));
}
