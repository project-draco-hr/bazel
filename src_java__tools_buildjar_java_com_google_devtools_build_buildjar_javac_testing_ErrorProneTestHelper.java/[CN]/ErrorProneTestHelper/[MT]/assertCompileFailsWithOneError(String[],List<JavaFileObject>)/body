{
  List<Diagnostic<? extends JavaFileObject>> diagnostics=assertCompileFails(args,sources);
  assertEquals("Expected 1 compiler message, found " + diagnostics.size() + ": "+ diagnostics,1,diagnostics.size());
  return diagnostics;
}
