{
  List<Diagnostic<? extends JavaFileObject>> diagnostics=assertCompileSucceeds(sources);
  for (  Diagnostic<? extends JavaFileObject> diagnostic : diagnostics) {
    assertWithMessage("Should not contain this warning").that(diagnostic.getMessage(null)).doesNotContainMatch(expectedErrorRegex);
  }
}
