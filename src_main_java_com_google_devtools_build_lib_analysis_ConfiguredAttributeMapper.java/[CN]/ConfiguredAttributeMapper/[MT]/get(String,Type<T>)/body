{
  try {
    return getAndValidate(attributeName,type);
  }
 catch (  EvalException e) {
    throw new IllegalStateException("lookup failed on attribute " + attributeName + ": "+ e.getMessage());
  }
}
