{
  createBzlFileForAlias();
  scratch.file("test/skylark/BUILD","load('/test/skylark/alias', one = 'four')","one(name = 'target')");
  reporter.removeHandler(failFastHandler);
  checkLoadAliasError("no such variable: four");
}
