{
  createBzlFileForAlias();
  scratch.file("test/skylark/BUILD","load('/test/skylark/alias', one = 'two', 'one')","one(name = 'target')");
  reporter.removeHandler(failFastHandler);
  checkLoadAliasError("Symbol 'one' has already been loaded");
}
