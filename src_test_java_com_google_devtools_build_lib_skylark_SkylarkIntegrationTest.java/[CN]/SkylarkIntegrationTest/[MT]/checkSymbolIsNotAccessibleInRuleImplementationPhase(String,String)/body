{
  scratch.file("test/skylark/extension.bzl","def custom_rule_impl(ctx):",ruleImplementationLines,"  return struct()","","custom_rule = rule(implementation = custom_rule_impl)");
  checkError("test/skylark","cr",symbolName + "() can only be called during the loading phase","load('/test/skylark/extension', 'custom_rule')","custom_rule(name = 'cr')");
}
