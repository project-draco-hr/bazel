{
  scratch.file("test/skylark/extension.bzl","def gen_list():","  return ['a']");
  scratch.file("test/skylark/BUILD","load('/test/skylark/extension', 'gen_list')","","l2 = gen_list() + ['b']","genrule(name = l2[0], cmd = 'echo hi >@', outs = ['a.txt'])","genrule(name = l2[1], cmd = 'echo hi >@', outs = ['b.txt'])");
  getConfiguredTarget("//test/skylark:a");
  getConfiguredTarget("//test/skylark:b");
}
