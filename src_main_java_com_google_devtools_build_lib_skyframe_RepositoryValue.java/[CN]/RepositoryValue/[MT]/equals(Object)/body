{
  if (this == other) {
    return true;
  }
  if (other == null || getClass() != other.getClass()) {
    return false;
  }
  RepositoryValue that=(RepositoryValue)other;
  return Objects.equal(repositoryName,that.repositoryName) && Objects.equal(repositoryDirectory,that.repositoryDirectory);
}
