{
  if (this == other) {
    return true;
  }
  if (other instanceof RepositoryValue) {
    RepositoryValue otherValue=(RepositoryValue)other;
    return details.equals(otherValue.details) && overlaidBuildFile.equals(otherValue.overlaidBuildFile);
  }
  return false;
}
