{
  ResolvedTargets<Target> result=parseTargetPatternList(parser,reporter,Arrays.asList(patterns),FilteringPolicies.NO_FILTER,false);
  return transformer.transformCompileOneDependency(reporter,result);
}
