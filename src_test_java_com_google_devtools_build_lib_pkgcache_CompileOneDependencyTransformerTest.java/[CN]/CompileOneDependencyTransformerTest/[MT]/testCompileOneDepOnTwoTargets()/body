{
  scratch.file("recursive/BUILD","filegroup(name = 'x', srcs = ['foox'])","filegroup(name = 'y', srcs = ['fooy'])");
  ResolvedTargets<Target> result=parseCompileOneDep("//recursive:foox","//recursive:fooy");
  assertFalse(result.hasError());
  assertThat(result.getTargets()).hasSize(2);
}
