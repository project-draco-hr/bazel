from tests.support import unittest2, inPy3k
from mock import call, _Call, create_autospec, MagicMock, Mock, ANY, _CallList, patch, PropertyMock
from datetime import datetime
if (__name__ == '__main__'):
    unittest2.main()
