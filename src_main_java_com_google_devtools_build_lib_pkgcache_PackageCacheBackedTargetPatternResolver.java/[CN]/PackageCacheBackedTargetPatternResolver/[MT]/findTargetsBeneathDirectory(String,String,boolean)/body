{
  FilteringPolicy actualPolicy=rulesOnly ? FilteringPolicies.and(FilteringPolicies.RULES_ONLY,policy) : policy;
  return findTargetsBeneathDirectory(eventHandler,originalPattern,pathPrefix,actualPolicy,keepGoing,pathPrefix.isEmpty());
}
