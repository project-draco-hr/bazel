{
  super.setUp();
  invalidationReceiver=new TrackingEvaluationProgressReceiver();
  builder=createBuilder(inMemoryCache,1,false,invalidationReceiver);
  executor=new DummyExecutor(rootDirectory);
}
