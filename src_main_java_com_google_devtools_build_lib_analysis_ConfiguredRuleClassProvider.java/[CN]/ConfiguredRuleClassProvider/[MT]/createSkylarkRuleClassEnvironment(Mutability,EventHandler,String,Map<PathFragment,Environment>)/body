{
  Environment env=Environment.builder(mutability).setSkylark().setGlobals(SkylarkModules.GLOBALS).setEventHandler(eventHandler).setFileContentHashCode(astFileContentHashCode).setImportedExtensions(importMap).setLoadingPhase().build();
  for (  Map.Entry<String,SkylarkType> entry : skylarkAccessibleJavaClasses.entrySet()) {
    env.setup(entry.getKey(),entry.getValue().getType());
  }
  return env;
}
