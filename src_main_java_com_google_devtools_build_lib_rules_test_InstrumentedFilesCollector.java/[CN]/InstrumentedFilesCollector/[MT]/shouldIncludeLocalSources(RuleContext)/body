{
  BuildConfiguration config=ruleContext.getConfiguration();
  return ((config.shouldInstrumentTestTargets() || !ruleContext.isTestTarget()) && config.getInstrumentationFilter().isIncluded(ruleContext.getLabel().toString()));
}
