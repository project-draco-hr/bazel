{
  try {
    Label.parseRepositoryLabel("foo//bar/baz:bat/boo");
    fail();
  }
 catch (  SyntaxException e) {
    assertEquals("invalid repository name 'foo': workspace name must start with '@'",e.getMessage());
  }
}
