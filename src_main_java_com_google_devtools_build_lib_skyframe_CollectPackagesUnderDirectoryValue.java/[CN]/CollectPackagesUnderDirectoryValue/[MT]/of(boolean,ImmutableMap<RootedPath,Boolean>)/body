{
  if (!isDirectoryPackage && subdirectoryTransitivelyContainsPackages.isEmpty()) {
    return EMPTY;
  }
  return new CollectPackagesUnderDirectoryValue(isDirectoryPackage,subdirectoryTransitivelyContainsPackages);
}
