{
  if (ruleContext.getPrerequisiteArtifact("manifest",Mode.TARGET) == null) {
    ruleContext.attributeError("manifest","is required when resource_files or assets are defined.");
    return false;
  }
  return true;
}
