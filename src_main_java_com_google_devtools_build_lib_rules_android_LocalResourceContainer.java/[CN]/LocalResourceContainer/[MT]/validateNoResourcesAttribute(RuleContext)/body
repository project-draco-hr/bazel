{
  if (ruleContext.attributes().isAttributeValueExplicitlySpecified("resources")) {
    ruleContext.attributeError("resources",String.format("cannot be set when any of %s are defined.",Joiner.on(", ").join(RESOURCES_ATTRIBUTES)));
    return false;
  }
  return true;
}
