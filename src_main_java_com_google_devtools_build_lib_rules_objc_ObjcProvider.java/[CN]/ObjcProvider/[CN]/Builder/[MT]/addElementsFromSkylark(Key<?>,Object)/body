{
  if (!(toAdd instanceof SkylarkNestedSet)) {
    throw new IllegalArgumentException(String.format(AppleSkylarkCommon.NOT_SET_ERROR,key.getSkylarkKeyName(),toAdd.getClass()));
  }
 else   if (!((SkylarkNestedSet)toAdd).getContentType().canBeCastTo(key.getType())) {
    throw new IllegalArgumentException(String.format(AppleSkylarkCommon.BAD_SET_TYPE_ERROR,key.getSkylarkKeyName(),key.getType(),((SkylarkNestedSet)toAdd).getContentType().getType()));
  }
 else {
    uncheckedAddAll(key,(SkylarkNestedSet)toAdd,this.items);
  }
}
