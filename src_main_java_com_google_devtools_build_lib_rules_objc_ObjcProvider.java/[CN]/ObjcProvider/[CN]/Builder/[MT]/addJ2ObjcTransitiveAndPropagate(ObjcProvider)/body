{
  addTransitiveAndPropagate(provider);
  for (  Map.Entry<Key<?>,NestedSet<?>> typeEntry : provider.items.entrySet()) {
    uncheckedAddTransitive(typeEntry.getKey(),typeEntry.getValue(),this.j2ObjcPropagatedItems);
  }
  return this;
}
