{
  if (OS.getCurrent() == OS.WINDOWS) {
    return ImmutableMap.of("PATH",cppConfiguration.getToolPathFragment(CppConfiguration.Tool.GCC).getParentDirectory().getPathString());
  }
  return ImmutableMap.of();
}
