{
  Function<Argument,String> function=new Function<Argument,String>(){
    @Override public String apply(    Argument id){
      return id.getArgName();
    }
  }
;
  return ImmutableList.copyOf(Lists.transform(args,function));
}
