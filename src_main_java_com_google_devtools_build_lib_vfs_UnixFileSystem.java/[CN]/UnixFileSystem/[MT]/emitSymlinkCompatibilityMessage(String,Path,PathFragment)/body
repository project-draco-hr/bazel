{
  if (symlinkLogFile == null) {
    return SymlinkImplementation.FAIL;
  }
  Exception e=new Exception();
  e.fillInStackTrace();
  String msg=String.format("ILLEGAL (%s): %s -> %s\nStack:\n%s",reason,linkPath.getPathString(),targetFragment.getPathString(),Throwables.getStackTraceAsString(e));
  writeLogMessage(msg);
  return SymlinkImplementation.SYMLINK;
}
