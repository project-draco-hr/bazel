{
  Preconditions.checkArgument(output.getParent().isTreeArtifact(),"Expanded set output must belong to a TreeArtifact");
  Set<ArtifactFile> values=getTreeArtifactContents(output.getParent());
  values.add(output);
}
