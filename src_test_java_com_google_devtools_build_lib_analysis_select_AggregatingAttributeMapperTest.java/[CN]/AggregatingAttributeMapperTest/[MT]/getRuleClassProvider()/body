{
  ConfiguredRuleClassProvider.Builder builder=new ConfiguredRuleClassProvider.Builder().addRuleDefinition(new RuleWithComputedDefaults());
  TestRuleClassProvider.addStandardRules(builder);
  return builder.build();
}
