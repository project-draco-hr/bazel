{
  return builder.requiresConfigurationFragments(AppleConfiguration.class).exemptFromConstraintChecking("this rule refines configuration variables and does not build actual content").add(attr(VERSION_ATTR_NAME,STRING).mandatory()).add(attr(ALIASES_ATTR_NAME,STRING_LIST)).build();
}
