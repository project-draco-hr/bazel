{
  String expanded=(String)evalRuleContextCode(ruleContext,String.format("ruleContext.expand_location('$(%s)')",command));
  assertTrue(String.format("Expanded string '%s' did not match pattern '%s'",expanded,expectedPattern),Pattern.matches(expectedPattern,expanded));
}
