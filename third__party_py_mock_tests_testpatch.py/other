import os
import sys
from tests import support
from tests.support import unittest2, inPy3k, SomeClass, is_instance, callable
from mock import NonCallableMock, CallableMixin, patch, sentinel, MagicMock, Mock, NonCallableMagicMock, patch, _patch, DEFAULT, call, _get_target
builtin_string = '__builtin__'
if inPy3k:
    builtin_string = 'builtins'
    unicode = str
PTModule = sys.modules[__name__]
MODNAME = ('%s.PTModule' % __name__)
something = sentinel.Something
something_else = sentinel.SomethingElse
foo_name = ('%s.Foo' % __name__)
if (__name__ == '__main__'):
    unittest2.main()
