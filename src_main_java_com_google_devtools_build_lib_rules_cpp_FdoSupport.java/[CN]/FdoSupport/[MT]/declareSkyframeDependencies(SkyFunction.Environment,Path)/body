{
  if (fdoProfile != null) {
    if (isLipoEnabled()) {
      PrecomputedValue.dependOnBuildId(env);
      return;
    }
    Path path;
    if (useAutoFdo) {
      path=fdoProfile.getParentDirectory().getRelative(fdoProfile.getBaseName() + ".imports");
    }
 else {
      path=fdoProfile;
    }
    env.getValue(FileValue.key(RootedPath.toRootedPathMaybeUnderRoot(path,ImmutableList.of(execRoot))));
  }
}
