{
  if (workerPool != null) {
    if (verbose) {
      env.getReporter().handle(Event.info("Build interrupted, shutting down worker pool..."));
    }
    workerPool.close();
    workerPool=null;
  }
}
