{
  if (workers != null) {
    if (verbose) {
      blazeRuntime.getReporter().handle(Event.info("Build interrupted, shutting down worker pool..."));
    }
    workers.close();
    workers=null;
  }
}
