{
  Preconditions.checkArgument(!reason.isEmpty());
  if (workerPool != null) {
    if (verbose) {
      env.getReporter().handle(Event.info(reason));
    }
    workerPool.close();
    workerPool=null;
  }
}
