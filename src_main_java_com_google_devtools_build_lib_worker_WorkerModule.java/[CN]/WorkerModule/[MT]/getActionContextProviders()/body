{
  Preconditions.checkNotNull(blazeRuntime);
  Preconditions.checkNotNull(buildRequest);
  Preconditions.checkNotNull(workers);
  return ImmutableList.<ActionContextProvider>of(new WorkerActionContextProvider(blazeRuntime,buildRequest,workers,blazeRuntime.getEventBus()));
}
