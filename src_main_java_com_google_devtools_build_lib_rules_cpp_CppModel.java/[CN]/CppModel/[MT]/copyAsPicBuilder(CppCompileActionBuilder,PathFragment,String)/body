{
  CppCompileActionBuilder picBuilder=new CppCompileActionBuilder(builder);
  picBuilder.addCopt("-fPIC").setOutputFile(ruleContext.getRelatedArtifact(outputName,".pic" + outputExtension)).setDotdFile(outputName,".pic.d",ruleContext);
  return picBuilder;
}
