{
  TransitiveInfoCollection libcLink=ruleContext.getPrerequisite(":libc_link",Mode.HOST);
  return libcLink != null ? libcLink.getProvider(FileProvider.class).getFilesToBuild() : NestedSetBuilder.<Artifact>emptySet(Order.STABLE_ORDER);
}
