{
  if (type != null && value != null) {
    if (!type.contains(value)) {
      throw new EvalException(loc,String.format("expected %s for '%s' while calling %s but got %s instead: %s",type,paramName,functionName,EvalUtils.getDataTypeName(value,true),value));
    }
  }
}
