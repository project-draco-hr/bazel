{
  return builder.requiresConfigurationFragments(ObjcConfiguration.class,AppleConfiguration.class).add(attr("binary",LABEL).allowedRuleClasses("apple_binary").allowedFileTypes().mandatory().direct_compile_time_input()).build();
}
