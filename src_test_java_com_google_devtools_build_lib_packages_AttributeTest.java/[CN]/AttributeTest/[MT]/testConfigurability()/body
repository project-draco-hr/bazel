{
  assertTrue(attr("foo_configurable",Type.LABEL_LIST).legacyAllowAnyFileType().build().isConfigurable());
  assertFalse(attr("foo_nonconfigurable",Type.LABEL_LIST).legacyAllowAnyFileType().nonconfigurable("test").build().isConfigurable());
}
