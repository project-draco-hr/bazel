{
  if (o == this) {
    return true;
  }
  if (!(o instanceof Root)) {
    return false;
  }
  Root r=(Root)o;
  return path.equals(r.path) && Objects.equals(execRoot,r.execRoot) && Objects.equals(isMainRepo,r.isMainRepo);
}
