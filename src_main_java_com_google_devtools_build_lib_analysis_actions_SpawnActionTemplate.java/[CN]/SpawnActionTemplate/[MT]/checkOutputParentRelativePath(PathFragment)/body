{
  Preconditions.checkArgument(parentRelativeOutputPath.isNormalized() && !parentRelativeOutputPath.isAbsolute(),"%s is not a proper relative path",parentRelativeOutputPath);
  return parentRelativeOutputPath;
}
