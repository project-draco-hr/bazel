{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof SymlinkToDirectory)) {
    return false;
  }
  return this.path.equals(((SymlinkToDirectory)obj).path) && Objects.equal(this.metadata,((SymlinkToDirectory)obj).metadata) && this.symlink.equals(((SymlinkToDirectory)obj).symlink);
}
