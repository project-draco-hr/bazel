{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof SymlinkToDirectory)) {
    return false;
  }
  return this.type.equals(((SymlinkToDirectory)obj).type) && this.path.equals(((SymlinkToDirectory)obj).path) && this.metadata.equals(((SymlinkToDirectory)obj).metadata)&& this.symlink.equals(((SymlinkToDirectory)obj).symlink);
}
