{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof RegularFile)) {
    return false;
  }
  return this.type.equals(((RegularFile)obj).type) && this.path.equals(((RegularFile)obj).path) && this.metadata.equals(((RegularFile)obj).metadata);
}
