{
  if (a == b) {
    return true;
  }
  if ((a == null) != (b == null)) {
    return false;
  }
  return a.getType().equals(b.getType()) && a.getPath().equals(b.getPath()) && a.getMetadata().equals(b.getMetadata());
}
