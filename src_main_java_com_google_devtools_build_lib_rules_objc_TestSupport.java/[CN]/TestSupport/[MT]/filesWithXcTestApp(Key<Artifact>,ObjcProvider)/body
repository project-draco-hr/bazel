{
  NestedSet<Artifact> underlying=objcProvider.get(key);
  XcTestAppProvider provider=ruleContext.getPrerequisite(IosTest.XCTEST_APP,Mode.TARGET,XcTestAppProvider.class);
  if (provider == null) {
    return underlying;
  }
  return NestedSetBuilder.<Artifact>stableOrder().addTransitive(underlying).addTransitive(provider.getObjcProvider().get(key)).build();
}
