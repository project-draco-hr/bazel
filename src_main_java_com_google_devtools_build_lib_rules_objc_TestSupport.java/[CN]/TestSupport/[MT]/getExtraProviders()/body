{
  if (ruleContext.getConfiguration().isCodeCoverageEnabled()) {
    return ImmutableMap.<Class<? extends TransitiveInfoProvider>,TransitiveInfoProvider>of(TestEnvironmentProvider.class,new TestEnvironmentProvider(gcovEnv()));
  }
  return ImmutableMap.of();
}
