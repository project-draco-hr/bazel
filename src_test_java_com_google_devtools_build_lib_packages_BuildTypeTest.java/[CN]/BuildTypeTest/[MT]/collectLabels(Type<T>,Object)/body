{
  ImmutableList.Builder<T> result=ImmutableList.builder();
  type.visitLabels(new Type.LabelVisitor(){
    @SuppressWarnings("unchecked") @Override public void visit(    Object object) throws InterruptedException {
      result.add((T)object);
    }
  }
,value);
  return result.build();
}
