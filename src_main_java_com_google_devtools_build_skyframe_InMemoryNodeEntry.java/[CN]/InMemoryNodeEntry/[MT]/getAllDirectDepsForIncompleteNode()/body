{
  Preconditions.checkState(!isDone(),this);
  if (!isDirty()) {
    return getTemporaryDirectDeps();
  }
 else {
    return Iterables.concat(getTemporaryDirectDeps(),buildingState.getAllRemainingDirtyDirectDeps());
  }
}
