{
  Preconditions.checkState(!isDone(),this);
  if (!isDirty()) {
    return Iterables.concat(getTemporaryDirectDeps());
  }
 else {
    return Iterables.concat(Iterables.concat(getTemporaryDirectDeps()),buildingState.getAllRemainingDirtyDirectDeps());
  }
}
