{
  if (packageLoader.isPackageDeleted(packageName) || LabelValidator.validatePackageName(packageName) != null) {
    return null;
  }
  return pkgLocator.get().getPackageBuildFileNullable(packageName,syscalls);
}
